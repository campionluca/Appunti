% Modulo 07 — Moduli e Package
\chapter{Moduli e Package}

\section{Introduzione}
Organizzare codice in moduli e package, import e percorso di ricerca.

Questa descrizione spiega come strutturare un progetto in unità coerenti, evitare conflitti di nomi e adottare convenzioni di import leggibili. Tocchiamo \texttt{\_\_init\_\_.py}, percorsi relativi/assoluti e il ruolo di \texttt{sys.path}.

Un buon design dei package rende il codice estensibile e manutenibile, facilitando test, riuso e distribuzione.

\section{Obiettivi di Apprendimento}
\begin{itemize}
  \item Creare e importare moduli.
  \item Strutturare package con \texttt{\_\_init\_\_.py}.
  \item Comprendere \texttt{sys.path} e import relativi.
\end{itemize}

\section{Concetti Fondamentali}
\begin{tcolorbox}[title=Import]
\texttt{from pkg.mod import func} e \texttt{import pkg.mod as m}
\end{tcolorbox}

\begin{tcolorbox}[title=Strutturazione e convenzioni]
La scelta tra \textbf{import assoluti e relativi} è importante per la chiarezza. Gli import assoluti dovrebbero essere preferiti poiché evitano ambiguità derivanti da import relativi profondi, rendendo il codice più leggibile e manutenibile.

Quando si organizzano package, è importante \textbf{mantenere } \texttt{\_\_init\_\_} \textbf{ minimale}. Il file dovrebbe contenere solo re-export selettivi e costanti del package, evitando logica complessa che rende difficile comprendere l'API.

La \textbf{progettazione dell'API di package} è fondamentale. Decidere consapevolmente cosa esporre dal livello radice e cosa mantenere come implementazione interna (tramite nome prefissato con underscore) crea un'interfaccia pulita per i consumatori.

Un principio importante è evitare \textbf{side-effect all'import}. L'import di un modulo non dovrebbe avere conseguenze osservabili come apertura di file o connessioni di database; queste operazioni dovrebbero essere inizializzate su richiesta esplicita.

Infine, \textbf{documentare le dipendenze interne} tra moduli riduce l'accoppiamento e facilita i refactoring futuri. Una struttura chiara aiuta a evitare cicli di dipendenza.
\end{tcolorbox}

\section{Esempi Pratici}

\subsubsection{Visualizzazione: Tipi di import}
\begin{center}
\begin{tikzpicture}[
  node distance=0.8cm,
  title/.style={rectangle, draw, fill=blue!30, text width=6cm, text centered, minimum height=0.8cm, font=\bfseries},
  import/.style={rectangle, draw, fill=green!10, text width=5.5cm, minimum height=0.6cm, font=\ttfamily\small, align=left},
  desc/.style={text width=5.5cm, font=\small\itshape, align=left}
]

\node[title] (abs) {Import Assoluti (Preferiti)};
\node[import, below=0.3cm of abs] (imp1) {import pkg.module};
\node[import, below=0.1cm of imp1] (imp2) {from pkg.module import func};
\node[import, below=0.1cm of imp2] (imp3) {from pkg import module};
\node[desc, below=0.2cm of imp3] (absdesc) {Chiari, espliciti, sempre funzionanti};

\node[title, below=1.2cm of absdesc] (rel) {Import Relativi (Uso Interno)};
\node[import, below=0.3cm of rel] (imp4) {from . import sibling};
\node[import, below=0.1cm of imp4] (imp5) {from .. import parent\_mod};
\node[import, below=0.1cm of imp5] (imp6) {from .subpkg import mod};
\node[desc, below=0.2cm of imp6] (reldesc) {Solo dentro package, evitano nomi lunghi};

\end{tikzpicture}
\end{center}

\subsection{Modulo semplice}
\begin{lstlisting}
# file: util.py
def say(msg):
    print(msg)

# file: app.py
import util
util.say("Ciao")
\end{lstlisting}

\begin{tcolorbox}[title=Spiegazione]
\begin{itemize}
  \item Il modulo \texttt{util.py} definisce una funzione e viene importato in \texttt{app.py}.
  \item \texttt{import util} carica il modulo e ne usa il namespace: \texttt{util.say(...)}.
  \item Preferire import espliciti (funzioni o nomi) quando la UX lo richiede: \texttt{from util import say}.
  \item Evitare side-effect all'import: l'esecuzione dovrebbe avvenire in \texttt{main()} o funzioni dedicate.
\end{itemize}
\end{tcolorbox}

\subsubsection{Visualizzazione: Esempio terminale}
\begin{center}
\begin{tcolorbox}[colback=black!5!white, colframe=black!75!black, title=Terminal Output, fonttitle=\ttfamily\bfseries]
\begin{verbatim}
$ python app.py
Ciao

$ python -c "from util import say; say('Hello World')"
Hello World
\end{verbatim}
\end{tcolorbox}
\end{center}

\subsection{Package}
\begin{lstlisting}
# pkg/__init__.py
# pkg/tools.py
def add(a, b):
    return a + b

# main.py
from pkg.tools import add
print(add(2, 3))
\end{lstlisting}

\begin{tcolorbox}[title=Spiegazione]
\begin{itemize}
  \item Un package è una cartella (\texttt{pkg/}) con moduli; \texttt{\_\_init\_\_.py} può essere vuoto o fare re-export.
  \item \texttt{from pkg.tools import add} importa simboli dal modulo \texttt{tools} all'interno del package.
  \item Mantenere \texttt{\_\_init\_\_.py} minimale e documentare l'API pubblica con \verb|__all__| se necessario.
  \item Preferire import assoluti per chiarezza; usare relativi solo all'interno del package.
\end{itemize}
\end{tcolorbox}

\subsubsection{Visualizzazione: Struttura directory package}
\begin{center}
\begin{tikzpicture}[
  level distance=1.2cm,
  sibling distance=3cm,
  edge from parent/.style={draw, -latex},
  every node/.style={font=\ttfamily\small, anchor=west},
  folder/.style={draw, rectangle, rounded corners, minimum width=2cm, minimum height=0.6cm, fill=blue!10},
  file/.style={draw, rectangle, minimum width=2cm, minimum height=0.6cm, fill=green!10}
]

\node[folder] {myproject/}
  child {node[file] {main.py}}
  child {node[folder] {pkg/}
    child {node[file] {\_\_init\_\_.py}}
    child {node[file] {tools.py}}
    child {node[file] {utils.py}}
    child {node[folder] {subpkg/}
      child {node[file] {\_\_init\_\_.py}}
      child {node[file] {module.py}}
    }
  };

\end{tikzpicture}
\end{center}

\subsubsection{Visualizzazione: Creazione package terminale}
\begin{center}
\begin{tcolorbox}[colback=black!5!white, colframe=black!75!black, title=Terminal Output, fonttitle=\ttfamily\bfseries]
\begin{verbatim}
$ mkdir -p myproject/pkg/subpkg
$ cd myproject
$ touch pkg/__init__.py pkg/tools.py
$ touch pkg/subpkg/__init__.py pkg/subpkg/module.py
$ tree
.
├── main.py
└── pkg
    ├── __init__.py
    ├── tools.py
    └── subpkg
        ├── __init__.py
        └── module.py

$ python main.py
5
\end{verbatim}
\end{tcolorbox}
\end{center}

\subsection{sys.path}
\begin{lstlisting}
import sys
print(sys.path)
\end{lstlisting}

\begin{tcolorbox}[title=Spiegazione]
\begin{itemize}
  \item \texttt{sys.path} è la lista di percorsi in cui Python cerca moduli e package.
  \item Modificare \texttt{sys.path} a runtime è possibile ma va limitato a script/CLI, non a librerie.
  \item Per la distribuzione, usare package installabili invece di aggiungere path manualmente.
\end{itemize}
\end{tcolorbox}

\subsubsection{Visualizzazione: Ordine di ricerca sys.path}
\begin{center}
\begin{tikzpicture}[
  node distance=1.5cm,
  box/.style={rectangle, draw, fill=blue!10, text width=7cm, text centered, rounded corners, minimum height=1cm},
  arrow/.style={-latex, thick}
]

\node[box] (step1) {1. Directory dello script corrente};
\node[box, below of=step1] (step2) {2. PYTHONPATH (variabile ambiente)};
\node[box, below of=step2] (step3) {3. Directory standard library};
\node[box, below of=step3] (step4) {4. Directory site-packages};
\node[box, below of=step4, fill=green!10] (found) {Modulo trovato!};
\node[box, below of=step4, fill=red!10, xshift=4cm] (notfound) {ModuleNotFoundError};

\draw[arrow] (step1) -- (step2);
\draw[arrow] (step2) -- (step3);
\draw[arrow] (step3) -- (step4);
\draw[arrow] (step4) -- node[right] {trovato} (found);
\draw[arrow] (step4.east) -- ++(1,0) |- node[near start, right] {non trovato} (notfound.east);

\end{tikzpicture}
\end{center}

\subsubsection{Visualizzazione: Esplorazione sys.path terminale}
\begin{center}
\begin{tcolorbox}[colback=black!5!white, colframe=black!75!black, title=Terminal Output, fonttitle=\ttfamily\bfseries]
\begin{verbatim}
$ python -c "import sys; print('\n'.join(sys.path))"

/usr/local/lib/python3.11.zip
/usr/local/lib/python3.11
/usr/local/lib/python3.11/lib-dynload
/usr/local/lib/python3.11/site-packages

$ python
>>> import sys
>>> sys.path.append('/custom/path')
>>> sys.path[-1]
'/custom/path'
\end{verbatim}
\end{tcolorbox}
\end{center}

\begin{tcolorbox}[title=Casi d'uso]
I moduli e package sono particolarmente utili per creare librerie interne di utilità condivise tra progetti scollegati, centralizzando funzionalità comuni e riducendo duplicazione di codice.

L'organizzazione di applicazioni complesse beneficia della suddivisione in sottopackage tematici come \texttt{core} per la logica centrale, \texttt{api} per le interfacce esterne e \texttt{cli} per comandi da linea di comando, migliorando la navigabilità e la manutenibilità.

La separazione di plugin ed estensioni in moduli dedicati caricabili dinamicamente permette di estendere le funzionalità dell'applicazione senza modificare il core, seguendo il principio Open/Closed.

Infine, il re-export dell'API pubblica dal package root migliora l'esperienza utente, permettendo import concisi come \verb|from mypackage import MyClass| invece di percorsi lunghi come \verb|from mypackage.internal.core import MyClass|.
\end{tcolorbox}

\section{Esercizi}
\begin{enumerate}
  \item Crea un package con due moduli e import incrociati.
  \item Implementa un modulo \texttt{mathutil} con funzioni di base.
  \item Usa alias negli import e confronta leggibilità.
  \item Esplora \texttt{sys.path} e aggiungi un percorso temporaneo.
  \item Organizza un progetto in package e sottopackage.
\end{enumerate}

\section{Riepilogo}
Hai organizzato codice con moduli e package in modo pulito.

\section{Contesto e Applicazioni}
\begin{tcolorbox}[title=Contesto e Applicazioni]
\begin{itemize}
  \item Organizzare progetti medio-grandi in package coerenti.
  \item Definire API pubblica del package con re-export.
  \item Isolare plugin/estensioni con moduli dedicati.
  \item Preparare distribuzione e riuso interno.
\end{itemize}
\end{tcolorbox}

\section{Approfondimenti}
\begin{tcolorbox}[title=Spiegazioni dettagliate]
La scelta tra \textbf{import assoluti e relativi} è una decisione di design importante. Import assoluti dovrebbero essere preferiti per la loro chiarezza e universalità; import relativi sono utili principalmente all'interno di pacchetti for evitare nomi lunghi, ma possono creare ambiguità se non usati con attenzione.

Il file \verb|__init__.py| è lo strumento principale per \textbf{controllare l'esposizione del pacchetto}. È possibile usare \verb|__all__| per definire esplicitamente l'API pubblica, facendo sì che solo i simboli elencati siano importabili con `from pkg import *`.

Una \textbf{struttura di moduli} ben progettata separa le responsabilità chiaramente e evita cicli di dipendenze che compromettono il mantenimento. Predisporre sottopacchetti coerenti che riflettono la struttura logica del progetto facilita la navigazione e la comprensione.

Infine, \textbf{ambienti e dipendenze} dovrebbero essere gestiti attentamente. Congelare le versioni delle dipendenze in un file di requisiti (come `requirements.txt`) e documentare vincoli di compatibilità assicura che il codice funzioni in modo prevedibile in diversi ambienti.
\end{tcolorbox}

\subsubsection{Visualizzazione: Flowchart risoluzione import}
\begin{center}
\begin{tikzpicture}[
  node distance=1.2cm and 2cm,
  start/.style={rectangle, rounded corners, draw, fill=green!20, text width=3cm, text centered, minimum height=1cm},
  process/.style={rectangle, draw, fill=blue!10, text width=3.5cm, text centered, minimum height=0.8cm},
  decision/.style={diamond, draw, fill=yellow!20, text width=2.5cm, text centered, aspect=2},
  end/.style={rectangle, rounded corners, draw, fill=red!20, text width=2.5cm, text centered, minimum height=0.8cm},
  arrow/.style={-latex, thick}
]

\node[start] (start) {import modulename};
\node[decision, below=of start] (cached) {In sys.modules?};
\node[end, right=of cached] (return) {Ritorna cached};
\node[process, below=of cached] (search) {Cerca in sys.path};
\node[decision, below=of search] (found) {Trovato?};
\node[process, right=of found] (load) {Carica modulo};
\node[process, below=of load] (cache) {Aggiungi a sys.modules};
\node[end, below=of found] (error) {ModuleNotFoundError};

\draw[arrow] (start) -- (cached);
\draw[arrow] (cached) -- node[above] {sì} (return);
\draw[arrow] (cached) -- node[right] {no} (search);
\draw[arrow] (search) -- (found);
\draw[arrow] (found) -- node[above] {sì} (load);
\draw[arrow] (load) -- (cache);
\draw[arrow] (cache) |- (return);
\draw[arrow] (found) -- node[right] {no} (error);

\end{tikzpicture}
\end{center}

\subsubsection{Visualizzazione: Esposizione API con \_\_all\_\_}
\begin{center}
\begin{tikzpicture}[
  node distance=1.5cm,
  package/.style={rectangle, draw, fill=blue!20, text width=5cm, minimum height=0.8cm, font=\ttfamily\small},
  public/.style={rectangle, draw, fill=green!20, text width=4cm, minimum height=0.6cm, font=\ttfamily\small},
  private/.style={rectangle, draw, fill=red!10, text width=4cm, minimum height=0.6cm, font=\ttfamily\small}
]

\node[package] (init) {pkg/\_\_init\_\_.py};
\node[below=0.3cm of init, anchor=north, font=\ttfamily\small] (all) {\_\_all\_\_ = ['func1', 'ClassA']};

\node[public, below=0.8cm of all] (func1) {def func1(): ...};
\node[public, below=0.4cm of func1] (classa) {class ClassA: ...};
\node[private, below=0.4cm of classa] (internal) {def \_internal(): ...};

\draw[-latex, thick, green!60!black] (all.south) -- ++(0,-0.3) -| (func1.north);
\draw[-latex, thick, green!60!black] (all.south) -- ++(0,-0.3) -| (classa.north);

\node[right=2cm of func1, text width=3cm] (exp1) {Esportato pubblicamente};
\node[right=2cm of classa] (exp2) {Esportato pubblicamente};
\node[right=2cm of internal, text width=3cm] (nexp) {NON esportato (privato)};

\draw[-latex, dashed] (func1) -- (exp1);
\draw[-latex, dashed] (classa) -- (exp2);
\draw[-latex, dashed, red] (internal) -- (nexp);

\end{tikzpicture}
\end{center}

\subsubsection{Visualizzazione: Esempio \_\_all\_\_ terminale}
\begin{center}
\begin{tcolorbox}[colback=black!5!white, colframe=black!75!black, title=Terminal Output, fonttitle=\ttfamily\bfseries]
\begin{verbatim}
$ cat pkg/__init__.py
__all__ = ['public_func']

def public_func():
    return "Public"

def _internal_func():
    return "Internal"

$ python
>>> from pkg import *
>>> public_func()
'Public'
>>> _internal_func()
NameError: name '_internal_func' is not defined
>>> from pkg import _internal_func  # Accesso esplicito
>>> _internal_func()
'Internal'
\end{verbatim}
\end{tcolorbox}
\end{center}

\paragraph{Distribuzione} Valutare packaging con \verb|pyproject.toml| e strumenti moderni; integrare test e lint nel processo di rilascio.
Consulta: \url{https://docs.python.org/3/tutorial/modules.html}.

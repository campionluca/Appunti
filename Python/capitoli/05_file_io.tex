% Modulo 05 — File e Gestione I/O
\chapter{File e Gestione I/O}

\section{Introduzione}
Operazioni di lettura/scrittura su file e gestione risorse con context manager.

Descriviamo flussi tipici di I/O: aprire file in sicurezza, trattare encoding e newline multipiattaforma, leggere dati strutturati (CSV) e scrivere output affidabile. Evidenziamo l'uso di \texttt{with} per garantire rilascio delle risorse.

Queste competenze abilitano pipeline di dati, logging persistente e integrazione con altri sistemi (configurazioni, esportazioni, archiviazione).

\section{Obiettivi di Apprendimento}
\begin{itemize}
  \item Aprire, leggere e scrivere file di testo e CSV.
  \item Usare \texttt{with} per gestire risorse.
  \item Gestire percorsi e encoding.
\end{itemize}

\section{Concetti Fondamentali}
\begin{tcolorbox}[title=Context Manager]
Usa \texttt{with open("file.txt", "r", encoding="utf-8") as f: ...}
\end{tcolorbox}
\begin{tcolorbox}[title=Percorsi e filesystem]
Preferisci \texttt{pathlib} per percorsi portabili (\texttt{Path("dir") / "file.txt"}). Attenzione a separatori e encoding tra sistemi; valida l'esistenza dei file e gestisci directory in modo sicuro.
\end{tcolorbox}

\subsubsection{Visualizzazione: Flusso Operazioni File I/O}
\begin{center}
\begin{tikzpicture}[
  node distance=1.2cm,
  process/.style={rectangle, rounded corners, draw=blue!60, fill=blue!10, thick, minimum width=3cm, minimum height=0.8cm, align=center},
  decision/.style={diamond, draw=orange!60, fill=orange!10, thick, minimum width=2.5cm, minimum height=1cm, align=center, aspect=2},
  state/.style={ellipse, draw=green!60, fill=green!10, thick, minimum width=2cm, align=center},
  arrow/.style={->, >=stealth, thick}
]

% Process flow
\node[state] (start) {Inizio};
\node[process, below=of start] (open) {open(file, mode)};
\node[decision, below=of open] (success) {Successo?};
\node[process, right=2cm of success] (error) {Gestione\\Eccezione};
\node[process, below=of success] (ops) {Operazioni I/O\\read/write};
\node[process, below=of ops] (close) {close()};
\node[state, below=of close] (end) {Fine};

% Arrows
\draw[arrow] (start) -- (open);
\draw[arrow] (open) -- (success);
\draw[arrow] (success) -- node[right, font=\small] {No} (error);
\draw[arrow] (success) -- node[left, font=\small] {Sì} (ops);
\draw[arrow] (ops) -- (close);
\draw[arrow] (close) -- (end);
\draw[arrow] (error) |- (end);

% With statement annotation
\node[draw=red!60, fill=red!5, thick, rounded corners, fit={(open) (ops) (close)}, inner sep=0.3cm, label={[red!60]above right:with statement}] {};

\end{tikzpicture}
\end{center}

\section{Esempi Pratici}
\subsection{Lettura file}
\begin{lstlisting}
with open("dati.txt", "r", encoding="utf-8") as f:
    for line in f:
        print(line.strip())
\end{lstlisting}
\begin{tcolorbox}[title=Spiegazione del codice]
\begin{itemize}
  \item \textbf{with open}: garantisce chiusura automatica del file, anche su eccezione.
  \item \textbf{Iterazione}: il file è iterabile per riga; `strip` rimuove newline/spazi ai bordi.
  \item \textbf{Encoding}: esplicitare `utf-8` evita problemi di piattaforma.
\end{itemize}
\end{tcolorbox}

\subsubsection{Visualizzazione: Context Manager (with statement)}
\begin{center}
\begin{tikzpicture}[
  node distance=1cm,
  phase/.style={rectangle, rounded corners, draw=blue!60, fill=blue!10, thick, minimum width=4cm, minimum height=0.8cm, align=left},
  action/.style={rectangle, draw=green!60, fill=green!5, thick, minimum width=3.5cm, minimum height=0.6cm, align=left, font=\small},
  arrow/.style={->, >=stealth, thick}
]

% Main flow
\node[phase] (enter) {\textbf{1. \_\_enter\_\_()}\\Acquisisce risorsa};
\node[action, below=0.5cm of enter] (enter_action) {• Apre file\\• Restituisce file object};

\node[phase, below=0.8cm of enter_action] (body) {\textbf{2. Blocco with}\\Esegue codice};
\node[action, below=0.5cm of body] (body_action) {• Legge/scrive dati\\• Può sollevare eccezioni};

\node[phase, below=0.8cm of body_action] (exit) {\textbf{3. \_\_exit\_\_()}\\Rilascia risorsa};
\node[action, below=0.5cm of exit] (exit_action) {• Chiude file\\• Sempre eseguito};

% Arrows
\draw[arrow] (enter) -- (enter_action);
\draw[arrow] (enter_action) -- (body);
\draw[arrow] (body) -- (body_action);
\draw[arrow] (body_action) -- (exit);
\draw[arrow] (exit) -- (exit_action);

% Exception path
\node[action, right=2cm of body_action, draw=red!60, fill=red!10] (exception) {Eccezione\\sollevata};
\draw[arrow, red!60, dashed] (body_action) -- (exception);
\draw[arrow, red!60, dashed] (exception) |- (exit);

\end{tikzpicture}
\end{center}

\subsubsection{Esempio Terminale: Lettura File}
\begin{tcolorbox}[title={\faTerminal\ Output}, colback=black!5, colframe=black!70]
\begin{verbatim}
$ python
>>> with open("dati.txt", "r", encoding="utf-8") as f:
...     for line in f:
...         print(line.strip())
...
Prima riga del file
Seconda riga del file
Terza riga del file
>>> # File automaticamente chiuso qui
\end{verbatim}
\end{tcolorbox}

\subsection{Scrittura file}
\begin{lstlisting}
with open("out.txt", "w", encoding="utf-8") as f:
    f.write("Hello\n")
\end{lstlisting}
\begin{tcolorbox}[title=Spiegazione del codice]
\begin{itemize}
  \item \textbf{Modalità}: `"w"` sovrascrive il file (crea se non esiste).
  \item \textbf{Buffer}: `write` aggiunge al buffer; flush/chiusura avvengono alla fine del `with`.
  \item \textbf{Newline}: usa \verb|\n| come separatore universale; LaTeX e Windows gestiscono conversioni.
\end{itemize}
\end{tcolorbox}

\subsubsection{Esempio Terminale: Scrittura File}
\begin{tcolorbox}[title={\faTerminal\ Output}, colback=black!5, colframe=black!70]
\begin{verbatim}
$ python
>>> with open("out.txt", "w", encoding="utf-8") as f:
...     f.write("Hello\n")
...     f.write("World\n")
...
6
6
>>> # Verifica contenuto
>>> with open("out.txt", "r", encoding="utf-8") as f:
...     print(f.read())
...
Hello
World
\end{verbatim}
\end{tcolorbox}

\subsubsection{Visualizzazione: Modalità di Accesso ai File}
\begin{center}
\begin{tabular}{|l|l|p{6cm}|}
\hline
\textbf{Modalità} & \textbf{Tipo} & \textbf{Descrizione} \\
\hline
\texttt{'r'} & Testo & Lettura (default). Errore se file non esiste. \\
\hline
\texttt{'w'} & Testo & Scrittura. Sovrascrive file esistente o crea nuovo. \\
\hline
\texttt{'a'} & Testo & Append. Aggiunge alla fine, crea se non esiste. \\
\hline
\texttt{'x'} & Testo & Creazione esclusiva. Errore se file esiste. \\
\hline
\texttt{'r+'} & Testo & Lettura e scrittura. File deve esistere. \\
\hline
\texttt{'w+'} & Testo & Lettura e scrittura. Sovrascrive o crea. \\
\hline
\texttt{'rb'} & Binario & Lettura binaria. \\
\hline
\texttt{'wb'} & Binario & Scrittura binaria. Sovrascrive o crea. \\
\hline
\texttt{'ab'} & Binario & Append binario. \\
\hline
\texttt{'rb+'} & Binario & Lettura e scrittura binaria. \\
\hline
\end{tabular}
\end{center}

\begin{tcolorbox}[title=Note sulle modalità]
\begin{itemize}
  \item \textbf{Testo vs Binario}: modalità testo gestiscono encoding e newline; binarie trattano byte grezzi.
  \item \textbf{Creazione esclusiva ('x')}: utile per evitare sovrascrittura accidentale.
  \item \textbf{Append ('a')}: il puntatore è alla fine; ogni write aggiunge, senza sovrascrivere.
  \item \textbf{Modalità miste ('+')}: permettono sia lettura che scrittura; attenzione alla posizione del puntatore.
\end{itemize}
\end{tcolorbox}

\subsection{CSV}
\begin{lstlisting}
import csv
rows = [("nome", "eta"), ("Ada", 27)]
with open("people.csv", "w", newline="", encoding="utf-8") as f:
    writer = csv.writer(f)
    writer.writerows(rows)
\end{lstlisting}
\begin{tcolorbox}[title=Spiegazione del codice]
\begin{itemize}
  \item \textbf{csv.writer}: gestisce quoting/separatori corretti; evitare `join` per CSV complessi.
  \item \textbf{newline=""}: delega al modulo CSV la gestione dei terminatori di riga.
  \item \textbf{Schema}: definire header coerenti e tipi attesi aumenta robustezza.
\end{itemize}
\end{tcolorbox}

\subsubsection{Esempio Terminale: Scrittura e Lettura CSV}
\begin{tcolorbox}[title={\faTerminal\ Output}, colback=black!5, colframe=black!70]
\begin{verbatim}
$ python
>>> import csv
>>> # Scrittura CSV
>>> rows = [("nome", "eta"), ("Ada", 27), ("Bob", 32)]
>>> with open("people.csv", "w", newline="", encoding="utf-8") as f:
...     writer = csv.writer(f)
...     writer.writerows(rows)
...
>>> # Lettura CSV
>>> with open("people.csv", "r", newline="", encoding="utf-8") as f:
...     reader = csv.DictReader(f)
...     for row in reader:
...         print(f"{row['nome']}: {row['eta']} anni")
...
Ada: 27 anni
Bob: 32 anni
\end{verbatim}
\end{tcolorbox}

\begin{tcolorbox}[title=Casi d'uso]
\begin{itemize}
  \item Pipeline di elaborazione file (log, CSV, esportazioni).
  \item Gestione configurazioni e archiviazione semplice.
  \item Generazione di report testuali e tracciamento attività.
\end{itemize}
\end{tcolorbox}

\section{Esercizi}
\begin{enumerate}
  \item Conta linee e parole in un file di testo.
  \item Copia il contenuto di un file in un altro.
  \item Leggi un CSV e stampa solo le righe con età \(> 30\).
  \item Scrivi un log con timestamp per ogni riga letta.
  \item Gestisci errori di file mancante in lettura.
\end{enumerate}

\section{Riepilogo}
Hai praticato I/O su file e uso di context manager.

\section{Contesto e Applicazioni}
\begin{tcolorbox}[title=Contesto e Applicazioni]
\begin{itemize}
  \item Lettura/scrittura CSV per piccoli ETL locali.
  \item Gestione log e rotazione file.
  \item Backup e archiviazione di configurazioni.
  \item Import/export per strumenti CLI.
\end{itemize}
\end{tcolorbox}

\section{Approfondimenti}
\begin{tcolorbox}[title=Spiegazioni dettagliate]
La scelta delle \textbf{modalità di apertura} è importante per operazioni I/O corrette. Le modalità \verb|'r'|, \verb|'w'| e \verb|'a'| lavorano con testo, mentre \verb|'rb'| e \verb|'wb'| con dati binari. È fondamentale scegliere il modo corretto in base alla natura dei dati.

L'uso del \textbf{context manager} con \verb|with open(...) as f| è non solo una best practice ma obbligatorio per la sicurezza e l'affidabilità. Garantisce che il file sia chiuso automaticamente, anche se si verifica un'eccezione, prevenendo perdite di risorse.

La gestione dell'\textbf{encoding} è critica per evitare errori di decodifica. Esplicitare \verb|encoding='utf-8'| è lo standard; per tollerare errori di encoding non perfetti, si può usare \verb|errors='replace'| o \verb|errors='ignore'|.

\subsubsection{Esempio Terminale: Gestione Encoding}
\begin{tcolorbox}[title={\faTerminal\ Output}, colback=black!5, colframe=black!70]
\begin{verbatim}
$ python
>>> # Scrittura con encoding UTF-8
>>> with open("unicode.txt", "w", encoding="utf-8") as f:
...     f.write("Café è buono ☕\n")
...     f.write("Prezzo: €3.50\n")
...
16
15
>>> # Lettura con encoding UTF-8
>>> with open("unicode.txt", "r", encoding="utf-8") as f:
...     print(f.read())
...
Café è buono ☕
Prezzo: €3.50

>>> # Gestione errori encoding
>>> with open("file.txt", "r", encoding="utf-8",
...           errors="replace") as f:
...     content = f.read()  # caratteri invalidi → �
\end{verbatim}
\end{tcolorbox}

Il \textbf{buffering e newline} controllano come i dati vengono trattenuti in memoria prima della scrittura su disco e come vengono interpretati i terminatori di linea. Gestire newlines correttamente è essenziale, specialmente quando si lavora con file CSV o su piattaforme diverse.

Per operazioni su \textbf{percorsi di file}, \verb|pathlib.Path| dovrebbe essere preferito a \verb|os.path|. Offre un'API molto più leggibile e composabile, con supporto per operazioni comuni come composizione di path, controllo di esistenza e iterazione su directory.
\end{tcolorbox}

\subsubsection{Visualizzazione: Operazioni pathlib.Path}
\begin{center}
\begin{tikzpicture}[
  node distance=1.5cm,
  box/.style={rectangle, draw=blue!60, fill=blue!5, thick, minimum width=3cm, minimum height=0.8cm, align=center},
  op/.style={rectangle, draw=green!60, fill=green!5, thick, minimum width=2.5cm, minimum height=0.6cm, align=center, font=\small},
  arrow/.style={->, >=stealth, thick}
]

% Central Path object
\node[box, fill=orange!10, draw=orange!60] (path) {Path("data/file.txt")};

% Operations around it
\node[op, above left=0.8cm and 2cm of path] (parent) {.parent\\Path("data")};
\node[op, above right=0.8cm and 2cm of path] (name) {.name\\"file.txt"};
\node[op, left=2.5cm of path] (exists) {.exists()\\True/False};
\node[op, right=2.5cm of path] (suffix) {.suffix\\".txt"};
\node[op, below left=0.8cm and 2cm of path] (mkdir) {.mkdir()\\Crea directory};
\node[op, below right=0.8cm and 2cm of path] (read) {.read\_text()\\Legge contenuto};

% Arrows
\draw[arrow, blue!60] (path) -- (parent);
\draw[arrow, blue!60] (path) -- (name);
\draw[arrow, blue!60] (path) -- (exists);
\draw[arrow, blue!60] (path) -- (suffix);
\draw[arrow, blue!60] (path) -- (mkdir);
\draw[arrow, blue!60] (path) -- (read);

% Composition example
\node[op, below=1.5cm of path, fill=purple!10, draw=purple!60] (comp) {Path("data") / "file.txt"};
\draw[arrow, purple!60, dashed] (comp) -- (path) node[midway, right, font=\tiny] {composizione};

\end{tikzpicture}
\end{center}

\begin{tcolorbox}[title=Per chi viene da C/Java/JS: filesystem e flussi]
\textbf{Percorsi}: usa `pathlib.Path` per comporre percorsi portabili (`Path("dir") / "file.txt"`).  
\textbf{Testo vs binario}: `"r"/"w"` per testo con encoding, `"rb"/"wb"` per dati binari grezzi.  
\textbf{Sicurezza}: gestisci eccezioni come `FileNotFoundError`, `PermissionError`; evita perdita di risorse usando `with`.  
\textbf{CSV/JSON}: usa moduli standard `csv` e `json`; definisci schema e validazione separata dalla lettura/scrittura.  
\textbf{Temporary files}: usa `tempfile` per file temporanei sicuri.

Esempi commentati:
\begin{lstlisting}
from pathlib import Path
import json, csv

base = Path("data")
base.mkdir(exist_ok=True)

# Scrivi JSON
payload = {"name": "Ada", "age": 27}
with (base / "user.json").open("w", encoding="utf-8") as f:
    json.dump(payload, f, ensure_ascii=False, indent=2)

# Leggi CSV
with (base / "people.csv").open("w", newline="", encoding="utf-8") as f:
    w = csv.writer(f)
    w.writerow(["name", "age"]) 
    w.writerow(["Ada", 27])

with (base / "people.csv").open("r", newline="", encoding="utf-8") as f:
    r = csv.DictReader(f)
    rows = list(r)
    print(rows)
\end{lstlisting}
\begin{tcolorbox}[title=Spiegazione del codice]
\begin{itemize}
  \item \textbf{pathlib}: `Path` compone percorsi portabili e leggibili.
  \item \textbf{json.dump}: scrive JSON con \verb|ensure_ascii=False| per Unicode e \verb|indent| per leggibilità.
  \item \textbf{csv.DictReader}: crea dizionari per riga, facilitando accesso per chiave.
  \item \textbf{mkdir(exist\_ok)}: idempotente; prepara cartella dati senza errori se già esiste.
\end{itemize}
\end{tcolorbox}

\subsubsection{Esempio Terminale: Operazioni pathlib}
\begin{tcolorbox}[title={\faTerminal\ Output}, colback=black!5, colframe=black!70]
\begin{verbatim}
$ python
>>> from pathlib import Path
>>> # Composizione percorsi
>>> base = Path("data")
>>> file_path = base / "test.txt"
>>> print(file_path)
data/test.txt
>>> # Creazione directory
>>> base.mkdir(exist_ok=True)
>>> # Controllo esistenza
>>> print(file_path.exists())
False
>>> # Scrittura con pathlib
>>> file_path.write_text("Hello from pathlib!\n", encoding="utf-8")
20
>>> print(file_path.exists())
True
>>> # Lettura con pathlib
>>> content = file_path.read_text(encoding="utf-8")
>>> print(content)
Hello from pathlib!

>>> # Informazioni sul file
>>> print(f"Nome: {file_path.name}")
Nome: test.txt
>>> print(f"Parent: {file_path.parent}")
Parent: data
>>> print(f"Suffix: {file_path.suffix}")
Suffix: .txt
\end{verbatim}
\end{tcolorbox}
\end{tcolorbox}

\begin{tcolorbox}[title={Idiomi: pathlib, context manager, validazione}]
L'uso di \textbf{pathlib} per comporre, controllare e creare percorsi è il moderno standard Python. Preferire `Path` a `os.path` rende il codice più leggibile e portabile across piattaforme.

I \textbf{context manager} con `with` dovrebbero essere usati per gestire file, socket e altre risorse. Riducono boilerplate e preveniscono bug dovuti a cleanup manual mancato.

Infine, la \textbf{validazione} è importante prima di elaborare file. Verificare l'esistenza dei file e i permessi di accesso prevenisce errori cryptici successivamente. Inoltre, è fondamentale isolare l'I/O dalla logica di trasformazione, permettendo di testare la logica indipendentemente dal filesystem.
\end{tcolorbox}

\paragraph{Pratiche} Validare dimensione file, esistenza e permessi prima di elaborare. Isolare l'I/O dai trasformazioni per semplificare test e riuso.
Consulta: \url{https://docs.python.org/3/tutorial/inputoutput.html}.

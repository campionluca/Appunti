% Modulo 11 — GUI con Tkinter
\chapter{GUI con Tkinter}

\begin{tcolorbox}[title=Obiettivi del capitolo]
Dopo questo capitolo saprai:
\begin{itemize}
  \item Creare finestre e dialog con Tkinter
  \item Usare widgets base (Label, Button, Entry, Text)
  \item Gestire layout con pack, grid, place
  \item Implementare eventi e callbacks
  \item Usare widgets avanzati (Combobox, Scale, Progressbar)
  \item Creare menu e dialogs
  \item Usare Canvas per grafica
  \item Threading per operazioni lunghe
  \item Applicare pattern MVC per GUI
\end{itemize}
\end{tcolorbox}

\section{Teoria}

Tkinter è la libreria GUI standard di Python, inclusa di default con l'installazione del linguaggio. Basata su Tk/Tcl, offre diverse caratteristiche che la rendono adatta a molti casi d'uso.

Dal punto di vista della \textbf{portabilità}, Tkinter funziona nativamente su Windows, macOS e Linux senza richiedere modifiche al codice, garantendo un'esperienza consistente across piattaforme.

La natura \textbf{lightweight} di Tkinter è un vantaggio significativo: non richiede dipendenze esterne oltre alla distribuzione standard di Python, semplificando deployment e distribuzione delle applicazioni.

L'architettura \textbf{event-driven} è il cuore di Tkinter: un main loop gestisce automaticamente gli eventi dell'interfaccia (click, tastiera, timer), delegando alle callback appropriate la gestione delle interazioni utente.

La \textbf{semplicità} di Tkinter la rende ideale per tool interni, prototipi rapidi e utility, permettendo di creare interfacce funzionali con poco codice.

Tuttavia, Tkinter presenta alcune \textbf{limitazioni} da considerare. Il look nativo moderno è disponibile solo attraverso i themed widgets (ttk), mentre i widget classici hanno un aspetto datato. Rispetto a framework più completi come PyQt o wxPython, Tkinter offre meno widget predefiniti e funzionalità avanzate. Infine, le performance possono degradare quando si gestiscono interfacce con centinaia di widget simultanei.

\subsubsection{Visualizzazione: Gerarchia Widget Tkinter}

\begin{center}
\begin{tikzpicture}[
    node distance=1.2cm and 1.5cm,
    widget/.style={rectangle, draw=blue!60, fill=blue!10, thick, minimum width=2.5cm, minimum height=0.8cm, font=\small},
    container/.style={rectangle, draw=green!60, fill=green!10, thick, minimum width=2.5cm, minimum height=0.8cm, font=\small},
    root/.style={rectangle, draw=red!60, fill=red!10, thick, minimum width=2.5cm, minimum height=0.8cm, font=\small\bfseries},
    arrow/.style={->, >=stealth, thick}
]

% Root
\node[root] (root) {Tk (Root)};

% Container widgets
\node[container, below left=of root, xshift=-2cm] (frame) {Frame};
\node[container, below=of root] (toplevel) {Toplevel};
\node[container, below right=of root, xshift=2cm] (labelframe) {LabelFrame};

% Basic widgets
\node[widget, below=of frame, xshift=-2cm] (label) {Label};
\node[widget, below=of frame, xshift=0cm] (button) {Button};
\node[widget, below=of frame, xshift=2cm] (entry) {Entry};

% More widgets
\node[widget, below=of toplevel, xshift=-1.5cm] (text) {Text};
\node[widget, below=of toplevel, xshift=0cm] (listbox) {Listbox};
\node[widget, below=of toplevel, xshift=1.5cm] (canvas) {Canvas};

% Advanced widgets
\node[widget, below=of labelframe, xshift=-1cm] (scale) {Scale};
\node[widget, below=of labelframe, xshift=1cm] (spinbox) {Spinbox};

% Arrows showing hierarchy
\draw[arrow] (root) -- (frame);
\draw[arrow] (root) -- (toplevel);
\draw[arrow] (root) -- (labelframe);

\draw[arrow] (frame) -- (label);
\draw[arrow] (frame) -- (button);
\draw[arrow] (frame) -- (entry);

\draw[arrow] (toplevel) -- (text);
\draw[arrow] (toplevel) -- (listbox);
\draw[arrow] (toplevel) -- (canvas);

\draw[arrow] (labelframe) -- (scale);
\draw[arrow] (labelframe) -- (spinbox);

% Legend
\node[root, right=of labelframe, xshift=3cm, yshift=1cm] (leg1) {Root Window};
\node[container, right=of labelframe, xshift=3cm, yshift=0cm] (leg2) {Containers};
\node[widget, right=of labelframe, xshift=3cm, yshift=-1cm] (leg3) {Widgets};

\end{tikzpicture}
\end{center}

\textbf{Note:} I widget container (Frame, Toplevel, LabelFrame) possono contenere altri widget. La finestra root (Tk) è il contenitore principale dell'applicazione.

\section{Finestra Base}

\begin{lstlisting}
import tkinter as tk

# Crea finestra principale
root = tk.Tk()
root.title("My Application")
root.geometry("400x300")  # Width x Height

# Aggiungi widget
label = tk.Label(root, text="Hello, Tkinter!")
label.pack()

# Start event loop
root.mainloop()
\end{lstlisting}

\subsubsection{Visualizzazione: Avvio Applicazione GUI}

\begin{tcolorbox}[title=Output Terminale]
\begin{verbatim}
$ python3 gui_app.py
[Una finestra GUI appare sullo schermo]

Finestra: "My Application"
+--------------------------------+
|  My Application           [_][□][x] |
+--------------------------------+
|                                |
|    Hello, Tkinter!             |
|                                |
|                                |
|                                |
|                                |
+--------------------------------+
Dimensioni: 400x300 pixel
\end{verbatim}
\end{tcolorbox}

\textbf{Note:} L'esecuzione di un'applicazione Tkinter blocca il terminale fino alla chiusura della finestra. Il metodo \verb|mainloop()| gestisce tutti gli eventi fino al termine dell'applicazione.

\section{Widgets Base}

\subsection{Label}

\begin{lstlisting}
import tkinter as tk

root = tk.Tk()

# Label con testo
label1 = tk.Label(root, text="Simple Label")
label1.pack()

# Label con styling
label2 = tk.Label(
    root,
    text="Styled Label",
    font=("Arial", 16, "bold"),
    fg="blue",
    bg="yellow"
)
label2.pack()

# Label con variabile dinamica
text_var = tk.StringVar()
text_var.set("Dynamic Text")

label3 = tk.Label(root, textvariable=text_var)
label3.pack()

# Update text
text_var.set("Updated Text!")

root.mainloop()
\end{lstlisting}

\subsection{Button}

\begin{lstlisting}
import tkinter as tk

def onClick():
    print("Button clicked!")
    label.config(text="Button was clicked")

root = tk.Tk()

label = tk.Label(root, text="Click the button")
label.pack()

button = tk.Button(
    root,
    text="Click Me",
    command=on_click,
    bg="green",
    fg="white",
    font=("Arial", 12)
)
button.pack()

root.mainloop()
\end{lstlisting}

\subsection{Entry (Text Input)}

\begin{lstlisting}
import tkinter as tk

def submit():
    text = entry.get()
    print(f"Input: {text}")
    result_label.config(text=f"You entered: {text}")
    entry.delete(0, tk.END)  # Clear

root = tk.Tk()

entry = tk.Entry(root, width=30)
entry.pack()

button = tk.Button(root, text="Submit", command=submit)
button.pack()

result_label = tk.Label(root, text="")
result_label.pack()

root.mainloop()
\end{lstlisting}

\subsection{Text (Multi-line)}

\begin{lstlisting}
import tkinter as tk

root = tk.Tk()

text = tk.Text(root, height=10, width=40)
text.pack()

# Insert text
text.insert(tk.END, "Line 1\n")
text.insert(tk.END, "Line 2\n")

# Get text
def getText():
    content = text.get("1.0", tk.END)  # From start to end
    print(content)

tk.Button(root, text="Get Text", command=get_text).pack()

root.mainloop()
\end{lstlisting}

\subsubsection{Visualizzazione: Showcase Widget Base}

\begin{center}
\begin{tikzpicture}[scale=0.9, transform shape]

% Label widget
\node[draw=black!60, fill=white, rectangle, minimum width=5cm, minimum height=1cm, anchor=west] at (0,7) {};
\node[anchor=west, font=\small] at (0.2,7) {Label: Simple Text};

% Styled Label
\node[draw=black!60, fill=yellow!30, rectangle, minimum width=5cm, minimum height=1cm, anchor=west] at (0,5.5) {};
\node[anchor=west, font=\Large\bfseries, blue] at (0.3,5.5) {Styled Label};

% Button
\node[draw=black!80, fill=green!40, rectangle, rounded corners=3pt, minimum width=3cm, minimum height=0.8cm, anchor=west] at (0,4) {};
\node[anchor=center, font=\small\bfseries, white] at (1.5,4) {Click Me};

% Entry widget
\node[draw=black!60, fill=white, rectangle, minimum width=5cm, minimum height=0.7cm, anchor=west] at (0,2.5) {};
\node[anchor=west, font=\small\ttfamily, gray] at (0.2,2.5) {Enter text here...};
\draw[gray, thick] (0.1,2.2) -- (0.1,2.8);  % Cursor

% Text widget (multiline)
\node[draw=black!60, fill=white, rectangle, minimum width=5cm, minimum height=2cm, anchor=west] at (0,0) {};
\node[anchor=north west, font=\small\ttfamily, align=left] at (0.2,0.9) {Line 1\\Line 2\\Line 3};

% Scrollbar
\draw[fill=gray!30, draw=black!60] (5.1,0) rectangle (5.4,1.8);
\draw[fill=gray!60, draw=black!80] (5.1,1.2) rectangle (5.4,1.6);

% Labels
\node[anchor=west, font=\footnotesize, gray] at (5.5,7) {Label};
\node[anchor=west, font=\footnotesize, gray] at (5.5,5.5) {Label con stile};
\node[anchor=west, font=\footnotesize, gray] at (5.5,4) {Button};
\node[anchor=west, font=\footnotesize, gray] at (5.5,2.5) {Entry (single line)};
\node[anchor=west, font=\footnotesize, gray] at (5.5,0.9) {Text (multiline)};

\end{tikzpicture}
\end{center}

\subsubsection{Visualizzazione: Anatomia di un Widget}

\begin{center}
\begin{tikzpicture}[scale=1]

% Outer margin
\draw[dashed, gray] (0,0) rectangle (6,3);
\node[anchor=north, font=\tiny, gray] at (3,0) {Margin (esterno)};

% Padding area
\draw[dashed, blue!50] (0.5,0.5) rectangle (5.5,2.5);
\node[anchor=north west, font=\tiny, blue] at (0.5,2.5) {Padding};

% Widget border
\draw[thick, black] (1,1) rectangle (5,2);

% Widget content
\node[draw=green!60, fill=green!20, rectangle, rounded corners=2pt, minimum width=3cm, minimum height=0.6cm] at (3,1.5) {Button Text};

% Annotations
\draw[<->, thick, red] (1,0.3) -- (5,0.3);
\node[below, font=\footnotesize, red] at (3,0.3) {width};

\draw[<->, thick, red] (5.7,1) -- (5.7,2);
\node[right, font=\footnotesize, red] at (5.7,1.5) {height};

% Padding arrows
\draw[<->, thick, blue] (0.5,0.8) -- (1,0.8);
\node[above, font=\tiny, blue] at (0.75,0.8) {padx};

\draw[<->, thick, blue] (0.8,0.5) -- (0.8,1);
\node[left, font=\tiny, blue] at (0.8,0.75) {pady};

% Border annotation
\draw[thick, orange] (4.9,1.9) -- (5.3,2.3);
\node[above right, font=\tiny, orange] at (5.3,2.3) {border};

\end{tikzpicture}
\end{center}

\textbf{Proprietà comuni dei widget:}
\begin{itemize}
  \item \textbf{width/height}: Dimensioni del widget (in pixel o caratteri)
  \item \textbf{padx/pady}: Spaziatura interna (tra bordo e contenuto)
  \item \textbf{bd/borderwidth}: Spessore del bordo
  \item \textbf{relief}: Stile del bordo (flat, raised, sunken, groove, ridge)
  \item \textbf{fg/foreground}: Colore testo
  \item \textbf{bg/background}: Colore sfondo
  \item \textbf{font}: Font (famiglia, dimensione, stile)
\end{itemize}

\section{Layout Managers}

\subsection{Pack}

\begin{lstlisting}
import tkinter as tk

root = tk.Tk()

# Stack vertically (default)
tk.Label(root, text="Top").pack()
tk.Label(root, text="Middle").pack()
tk.Label(root, text="Bottom").pack()

# Side by side
tk.Label(root, text="Left").pack(side=tk.LEFT)
tk.Label(root, text="Right").pack(side=tk.RIGHT)

# With padding
tk.Label(root, text="Padded").pack(padx=20, pady=10)

root.mainloop()
\end{lstlisting}

\subsection{Grid}

\begin{lstlisting}
import tkinter as tk

root = tk.Tk()

# Grid layout (row, column)
tk.Label(root, text="Name:").grid(row=0, column=0, sticky=tk.W)
tk.Entry(root).grid(row=0, column=1)

tk.Label(root, text="Email:").grid(row=1, column=0, sticky=tk.W)
tk.Entry(root).grid(row=1, column=1)

tk.Label(root, text="Password:").grid(row=2, column=0, sticky=tk.W)
tk.Entry(root, show="*").grid(row=2, column=1)

tk.Button(root, text="Submit").grid(row=3, column=0, columnspan=2)

root.mainloop()
\end{lstlisting}

\subsubsection{Visualizzazione: Confronto Layout Managers}

\begin{center}
\begin{tikzpicture}[scale=0.85, transform shape]

% PACK Layout
\node[font=\bfseries] at (2,7.5) {Pack Layout};

% Pack - vertical (default)
\node[anchor=west, font=\small] at (0,6.8) {Verticale (default):};
\draw[thick] (0,4) rectangle (4,6.5);
\node[draw=blue!60, fill=blue!20, rectangle, minimum width=3.5cm, minimum height=0.6cm] at (2,6) {Top};
\node[draw=blue!60, fill=blue!20, rectangle, minimum width=3.5cm, minimum height=0.6cm] at (2,5.2) {Middle};
\node[draw=blue!60, fill=blue!20, rectangle, minimum width=3.5cm, minimum height=0.6cm] at (2,4.4) {Bottom};

% Pack - horizontal
\node[anchor=west, font=\small] at (0,3.5) {Orizzontale (side=LEFT):};
\draw[thick] (0,2) rectangle (4,3.2);
\node[draw=green!60, fill=green!20, rectangle, minimum width=1cm, minimum height=0.8cm] at (0.6,2.6) {L};
\node[draw=green!60, fill=green!20, rectangle, minimum width=1cm, minimum height=0.8cm] at (1.8,2.6) {M};
\node[draw=green!60, fill=green!20, rectangle, minimum width=1cm, minimum height=0.8cm] at (3,2.6) {R};

% GRID Layout
\node[font=\bfseries] at (8,7.5) {Grid Layout};

\node[anchor=west, font=\small] at (5.5,6.8) {Griglia (row, column):};
\draw[thick] (5.5,4) rectangle (10.5,6.5);

% Grid lines
\draw[gray, dashed] (7,4) -- (7,6.5);
\draw[gray, dashed] (8.5,4) -- (8.5,6.5);
\draw[gray, dashed] (5.5,5.25) -- (10.5,5.25);

% Grid cells with labels
\node[draw=orange!60, fill=orange!20, rectangle, minimum width=1.2cm, minimum height=0.9cm, font=\tiny] at (6.25,5.875) {(0,0)};
\node[draw=orange!60, fill=orange!20, rectangle, minimum width=1.2cm, minimum height=0.9cm, font=\tiny] at (7.75,5.875) {(0,1)};
\node[draw=orange!60, fill=orange!20, rectangle, minimum width=1.7cm, minimum height=0.9cm, font=\tiny] at (9.5,5.875) {(0,2) span=2};

\node[draw=orange!60, fill=orange!20, rectangle, minimum width=1.2cm, minimum height=0.9cm, font=\tiny] at (6.25,4.625) {(1,0)};
\node[draw=orange!60, fill=orange!20, rectangle, minimum width=1.2cm, minimum height=0.9cm, font=\tiny] at (7.75,4.625) {(1,1)};
\node[draw=orange!60, fill=orange!20, rectangle, minimum width=1.2cm, minimum height=0.9cm, font=\tiny] at (9,4.625) {(1,2)};
\node[draw=orange!60, fill=orange!20, rectangle, minimum width=1.2cm, minimum height=0.9cm, font=\tiny] at (10,4.625) {(1,3)};

% PLACE Layout
\node[font=\bfseries] at (8,3.5) {Place Layout};

\node[anchor=west, font=\small] at (5.5,2.8) {Posizione assoluta (x, y):};
\draw[thick] (5.5,0) rectangle (10.5,2.5);

% Placed widgets at specific coordinates
\node[draw=red!60, fill=red!20, rectangle, minimum width=1.5cm, minimum height=0.6cm, font=\tiny] at (6.5,2) {x=50, y=150};
\node[draw=red!60, fill=red!20, rectangle, minimum width=1.5cm, minimum height=0.6cm, font=\tiny] at (8.5,1) {x=150, y=80};
\node[draw=red!60, fill=red!20, rectangle, minimum width=1cm, minimum height=1cm, font=\tiny] at (9.5,0.5) {relx=0.8\\rely=0.2};

% Coordinate system
\draw[->, gray] (5.5,0) -- (5.5,0.5);
\node[left, font=\tiny, gray] at (5.5,0.25) {y};
\draw[->, gray] (5.5,0) -- (6,0);
\node[below, font=\tiny, gray] at (5.75,0) {x};

\end{tikzpicture}
\end{center}

\subsubsection{Visualizzazione: Pack Options}

\begin{center}
\begin{tikzpicture}[scale=0.8, transform shape]

% fill=NONE (default)
\node[font=\small\bfseries] at (2,5.5) {fill=NONE};
\draw[thick] (0,3.5) rectangle (4,5);
\node[draw=blue!60, fill=blue!20, rectangle, minimum width=1.5cm, minimum height=0.5cm] at (2,4.25) {Widget};
\node[font=\tiny, gray, align=center] at (2,3.2) {Widget mantiene\\dimensione naturale};

% fill=X
\node[font=\small\bfseries] at (7,5.5) {fill=X};
\draw[thick] (5,3.5) rectangle (9,5);
\node[draw=green!60, fill=green!20, rectangle, minimum width=3.5cm, minimum height=0.5cm] at (7,4.25) {Widget};
\node[font=\tiny, gray, align=center] at (7,3.2) {Si espande\\orizzontalmente};

% fill=Y
\node[font=\small\bfseries] at (2,2.5) {fill=Y};
\draw[thick] (0,0) rectangle (4,2);
\node[draw=orange!60, fill=orange!20, rectangle, minimum width=1.5cm, minimum height=1.5cm, align=center] at (2,1) {Widget};
\node[font=\tiny, gray, align=center] at (2,-0.3) {Si espande\\verticalmente};

% fill=BOTH
\node[font=\small\bfseries] at (7,2.5) {fill=BOTH};
\draw[thick] (5,0) rectangle (9,2);
\node[draw=red!60, fill=red!20, rectangle, minimum width=3.5cm, minimum height=1.5cm, align=center] at (7,1) {Widget};
\node[font=\tiny, gray, align=center] at (7,-0.3) {Si espande in\\entrambe direzioni};

\end{tikzpicture}
\end{center}

\subsubsection{Visualizzazione: Grid Spanning e Sticky}

\begin{center}
\begin{tikzpicture}[scale=0.9, transform shape]

% Grid with spanning
\node[font=\small\bfseries] at (3,5.5) {columnspan e rowspan};
\draw[thick] (0,2.5) rectangle (6,5);

% Grid lines
\draw[gray, dashed] (2,2.5) -- (2,5);
\draw[gray, dashed] (4,2.5) -- (4,5);
\draw[gray, dashed] (0,3.75) -- (6,3.75);

% Widgets
\node[draw=blue!60, fill=blue!20, rectangle, minimum width=5.5cm, minimum height=0.9cm, font=\small] at (3,4.375) {columnspan=3};
\node[draw=green!60, fill=green!20, rectangle, minimum width=1.7cm, minimum height=0.9cm, font=\tiny] at (1,3.125) {Normal};
\node[draw=orange!60, fill=orange!20, rectangle, minimum width=1.7cm, minimum height=0.9cm, font=\tiny] at (3,3.125) {Normal};
\node[draw=red!60, fill=red!20, rectangle, minimum width=1.7cm, minimum height=0.9cm, font=\tiny] at (5,3.125) {Normal};

% Sticky options
\node[font=\small\bfseries] at (10,5.5) {sticky (allineamento)};
\draw[thick] (7.5,2.5) rectangle (12.5,5);

% Grid cells
\draw[gray, dashed] (10,2.5) -- (10,5);
\draw[gray, dashed] (7.5,3.75) -- (12.5,3.75);

% Sticky demonstrations
\node[draw=blue!60, fill=blue!20, rectangle, minimum width=0.8cm, minimum height=0.4cm, font=\tiny] at (7.8,4.55) {NW};
\node[draw=green!60, fill=green!20, rectangle, minimum width=2cm, minimum height=0.4cm, font=\tiny] at (10,4.375) {N (top center)};

\node[draw=orange!60, fill=orange!20, rectangle, minimum width=0.5cm, minimum height=1cm, font=\tiny] at (8.75,3.125) {W};
\node[draw=red!60, fill=red!20, rectangle, minimum width=2.2cm, minimum height=1cm, font=\tiny] at (10.65,3.125) {NSEW (fill)};

% Compass
\node[font=\tiny, gray] at (7.5,5.2) {N};
\node[font=\tiny, gray] at (7.3,3.75) {W};
\node[font=\tiny, gray] at (7.5,2.3) {S};
\node[font=\tiny, gray] at (7.7,3.75) {E};

\end{tikzpicture}
\end{center}

\textbf{Riepilogo Layout Managers:}

\begin{center}
\begin{tabular}{|l|p{4cm}|p{5cm}|}
\hline
\textbf{Manager} & \textbf{Quando usarlo} & \textbf{Caratteristiche} \\
\hline
\textbf{pack} & Layout semplici, lineari & Facile e veloce. Widget impilati verticalmente/orizzontalmente. \\
\hline
\textbf{grid} & Form, tabelle, layout strutturati & Preciso e flessibile. Organizzazione in righe/colonne. Ideale per form. \\
\hline
\textbf{place} & Posizionamento assoluto & Controllo pixel-perfect. Poco flessibile al resize. Usare raramente. \\
\hline
\end{tabular}
\end{center}

\textbf{IMPORTANTE:} Non mescolare pack() e grid() nello stesso parent container! Causa comportamenti imprevedibili.

\section{Eventi e Bindings}

\begin{lstlisting}
import tkinter as tk

def onKey(event):
    print(f"Key pressed: {event.char}")

def onMouseClick(event):
    print(f"Mouse clicked at ({event.x}, {event.y})")

root = tk.Tk()

# Bind keyboard
root.bind("<Key>", on_key)
root.bind("<Return>", lambda e: print("Enter pressed"))

# Bind mouse
canvas = tk.Canvas(root, width=300, height=200, bg="white")
canvas.pack()
canvas.bind("<Button-1>", on_mouse_click)  # Left click

root.mainloop()
\end{lstlisting}

\subsubsection{Visualizzazione: Event Handling Flow}

\begin{center}
\begin{tikzpicture}[
    node distance=1.5cm,
    event/.style={rectangle, rounded corners, draw=blue!60, fill=blue!10, thick, minimum width=3cm, minimum height=0.8cm, font=\small},
    action/.style={rectangle, draw=green!60, fill=green!10, thick, minimum width=3cm, minimum height=0.8cm, font=\small},
    decision/.style={diamond, draw=orange!60, fill=orange!10, thick, minimum width=2cm, minimum height=1.2cm, font=\tiny, aspect=2},
    arrow/.style={->, >=stealth, thick}
]

% Flow
\node[event] (user) {Azione Utente};
\node[event, below=of user] (os) {Sistema Operativo};
\node[event, below=of os] (tkinter) {Tkinter mainloop()};
\node[decision, below=of tkinter] (bound) {Evento\\ bound?};
\node[action, below left=of bound, xshift=-1cm] (ignore) {Ignora};
\node[action, below right=of bound, xshift=1cm] (callback) {Chiama callback};
\node[action, below=of callback] (update) {Aggiorna UI};
\node[event, below=of update, yshift=-0.5cm] (loop) {Torna a mainloop};

% Arrows
\draw[arrow] (user) -- (os) node[midway, right, font=\tiny] {click/key};
\draw[arrow] (os) -- (tkinter) node[midway, right, font=\tiny] {evento};
\draw[arrow] (tkinter) -- (bound);
\draw[arrow] (bound) -- (ignore) node[midway, above, font=\tiny] {No};
\draw[arrow] (bound) -- (callback) node[midway, above, font=\tiny] {Si};
\draw[arrow] (callback) -- (update);
\draw[arrow] (update) -- (loop);
\draw[arrow] (ignore) |- (loop);

% Loop back
\draw[arrow, dashed] (loop) -- ++(3,0) |- (tkinter);

\end{tikzpicture}
\end{center}

\subsubsection{Visualizzazione: Event Bindings Comuni}

\begin{center}
\begin{tabular}{|l|l|p{5cm}|}
\hline
\textbf{Event} & \textbf{Binding} & \textbf{Descrizione} \\
\hline
\multicolumn{3}{|c|}{\textbf{Tastiera}} \\
\hline
\verb|<Key>| & Qualsiasi tasto & Cattura ogni pressione tasto \\
\verb|<Return>| & Enter & Tasto Invio \\
\verb|<Escape>| & Esc & Tasto Escape \\
\verb|<space>| & Spazio & Barra spaziatrice \\
\verb|<BackSpace>| & Backspace & Cancella indietro \\
\verb|<Control-c>| & Ctrl+C & Combinazione tasti \\
\hline
\multicolumn{3}{|c|}{\textbf{Mouse}} \\
\hline
\verb|<Button-1>| & Click sinistro & Mouse button sinistro \\
\verb|<Button-2>| & Click centrale & Mouse button centrale \\
\verb|<Button-3>| & Click destro & Mouse button destro \\
\verb|<Double-Button-1>| & Doppio click & Doppio click sinistro \\
\verb|<Motion>| & Movimento & Mouse si muove \\
\verb|<B1-Motion>| & Drag & Movimento con button premuto \\
\hline
\multicolumn{3}{|c|}{\textbf{Window}} \\
\hline
\verb|<FocusIn>| & Focus & Widget riceve focus \\
\verb|<FocusOut>| & Blur & Widget perde focus \\
\verb|<Configure>| & Resize & Finestra ridimensionata \\
\verb|<Destroy>| & Chiusura & Widget distrutto \\
\hline
\end{tabular}
\end{center}

\subsubsection{Visualizzazione: Esempio Event Handling}

\begin{tcolorbox}[title=Output Terminale - Event Handling]
\begin{verbatim}
$ python3 event_demo.py

[Finestra GUI aperta - utente interagisce]

Key pressed: a
Key pressed: b
Key pressed: c
Enter pressed
Mouse clicked at (120, 85)
Mouse clicked at (245, 150)
Mouse clicked at (300, 200)

[Utente chiude la finestra]
$
\end{verbatim}
\end{tcolorbox}

\section{Widgets Avanzati (ttk)}

\begin{lstlisting}
import tkinter as tk
from tkinter import ttk

root = tk.Tk()

# Combobox
combo = ttk.Combobox(root, values=["Python", "Java", "C++"])
combo.set("Python")
combo.pack()

# Progressbar
progress = ttk.Progressbar(root, length=200, mode="determinate")
progress["maximum"] = 100
progress["value"] = 50
progress.pack()

# Spinbox
spinbox = tk.Spinbox(root, from_=0, to=100, increment=5)
spinbox.pack()

# Scale (Slider)
scale = tk.Scale(root, from_=0, to=100, orient=tk.HORIZONTAL)
scale.pack()

def showValue():
    print(f"Scale value: {scale.get()}")

tk.Button(root, text="Show Value", command=show_value).pack()

root.mainloop()
\end{lstlisting}

\section{Menu}

\begin{lstlisting}
import tkinter as tk
from tkinter import messagebox

def newFile():
    messagebox.showinfo("New", "New file created")

def openFile():
    messagebox.showinfo("Open", "Open file dialog")

def exitApp():
    root.quit()

root = tk.Tk()

# Create menubar
menubar = tk.Menu(root)
root.config(menu=menubar)

# File menu
file_menu = tk.Menu(menubar, tearoff=0)
menubar.add_cascade(label="File", menu=file_menu)
file_menu.add_command(label="New", command=newFile)
file_menu.add_command(label="Open", command=openFile)
file_menu.add_separator()
file_menu.add_command(label="Exit", command=exitApp)

# Edit menu
edit_menu = tk.Menu(menubar, tearoff=0)
menubar.add_cascade(label="Edit", menu=edit_menu)
edit_menu.add_command(label="Cut", command=lambda: print("Cut"))  # Output (al click): Cut
edit_menu.add_command(label="Copy", command=lambda: print("Copy"))  # Output (al click): Copy

root.mainloop()
\end{lstlisting}

\section{Dialogs}

\begin{lstlisting}
import tkinter as tk
from tkinter import messagebox, filedialog, simpledialog

root = tk.Tk()

def showInfo():
    messagebox.showinfo("Info", "This is info")

def showWarning():
    messagebox.showwarning("Warning", "This is warning")

def showError():
    messagebox.showerror("Error", "This is error")

def askQuestion():
    result = messagebox.askyesno("Question", "Do you like Python?")
    print(f"Answer: {result}")

def openFile():
    filename = filedialog.askopenfilename(
        title="Select file",
        filetypes=[("Text files", "*.txt"), ("All files", "*.*")]
    )
    print(f"Selected: {filename}")

def askName():
    name = simpledialog.askstring("Input", "Enter your name:")
    print(f"Name: {name}")

tk.Button(root, text="Info", command=show_info).pack()
tk.Button(root, text="Warning", command=show_warning).pack()
tk.Button(root, text="Error", command=show_error).pack()
tk.Button(root, text="Question", command=ask_question).pack()
tk.Button(root, text="Open File", command=open_file).pack()
tk.Button(root, text="Ask Name", command=ask_name).pack()

root.mainloop()
\end{lstlisting}

\section{Canvas: Grafica}

\begin{lstlisting}
import tkinter as tk

root = tk.Tk()

canvas = tk.Canvas(root, width=400, height=300, bg="white")
canvas.pack()

# Draw shapes
canvas.create_line(0, 0, 400, 300, fill="red", width=2)
canvas.create_rectangle(50, 50, 150, 100, fill="blue", outline="black")
canvas.create_oval(200, 50, 300, 150, fill="green")
canvas.create_polygon(100, 200, 150, 250, 50, 250, fill="yellow")

# Draw text
canvas.create_text(200, 270, text="Hello Canvas!", font=("Arial", 16))

root.mainloop()
\end{lstlisting}

\subsubsection{Visualizzazione: Sistema di Coordinate Canvas}

\begin{center}
\begin{tikzpicture}[scale=0.8]

% Canvas border
\draw[thick] (0,0) rectangle (10,7);
\node[above] at (5,7) {Canvas (400x300)};

% Coordinate system
\draw[->, very thick, red] (0,0) -- (1.5,0);
\node[below, red] at (0.75,0) {X};
\draw[->, very thick, red] (0,0) -- (0,1.5);
\node[left, red] at (0,0.75) {Y};

% Origin
\node[circle, fill=red, inner sep=2pt, label=above right:{(0,0)}] at (0,0) {};

% Grid
\foreach \x in {2,4,6,8} {
    \draw[gray, dotted] (\x,0) -- (\x,7);
    \node[below, font=\tiny, gray] at (\x,0) {\x 00};
}
\foreach \y in {1,2,3,4,5,6} {
    \draw[gray, dotted] (0,\y) -- (10,\y);
    \node[left, font=\tiny, gray] at (0,\y) {\y 0};
}

% Example shapes with coordinates
% Line
\draw[thick, blue] (0,0) -- (10,7);
\node[circle, fill=blue, inner sep=1.5pt] at (0,0) {};
\node[circle, fill=blue, inner sep=1.5pt] at (10,7) {};
\node[above left, blue, font=\tiny] at (10,7) {(400,300)};

% Rectangle
\draw[thick, green!60, fill=green!20] (1,1) rectangle (3.5,2.5);
\node[circle, fill=green!60, inner sep=1pt] at (1,1) {};
\node[circle, fill=green!60, inner sep=1pt] at (3.5,2.5) {};
\node[below, green!60, font=\tiny] at (1,1) {(50,50)};
\node[above, green!60, font=\tiny] at (3.5,2.5) {(150,100)};

% Oval
\draw[thick, orange!80, fill=orange!20] (5,1.25) ellipse (1.25 and 1.25);
\node[circle, fill=orange!80, inner sep=1pt] at (3.75,0) {};
\node[circle, fill=orange!80, inner sep=1pt] at (6.25,2.5) {};
\node[below right, orange!80, font=\tiny] at (3.75,0) {(200,50)};
\node[above, orange!80, font=\tiny] at (6.25,2.5) {(300,150)};

% Polygon
\draw[thick, purple!80, fill=purple!20] (2.5,5) -- (3.75,6.25) -- (1.25,6.25) -- cycle;
\node[above, purple!80, font=\tiny] at (2.5,6.3) {polygon};

% Text
\node[font=\small] at (5,6.75) {Hello Canvas!};
\node[circle, fill=black, inner sep=1pt] at (5,6.75) {};
\node[above, font=\tiny] at (5,6.75) {(200,270)};

\end{tikzpicture}
\end{center}

\textbf{Coordinate Canvas:}
\begin{itemize}
  \item L'origine (0,0) è in alto a sinistra
  \item X aumenta verso destra
  \item Y aumenta verso il basso (opposto al piano cartesiano)
  \item Le coordinate sono in pixel
\end{itemize}

\subsubsection{Visualizzazione: Canvas Shapes Reference}

\begin{center}
\begin{tikzpicture}[scale=0.9]

% Line
\node[font=\small\bfseries] at (2,6.5) {Line};
\draw[very thick, red] (0,5.5) -- (4,4.5);
\node[font=\tiny, align=left] at (2,4) {create\_line(x1, y1, x2, y2,\\fill="red", width=2)};

% Rectangle
\node[font=\small\bfseries] at (2,3) {Rectangle};
\draw[thick, blue, fill=blue!20] (0.5,1.5) rectangle (3.5,2.5);
\node[font=\tiny, align=left] at (2,0.8) {create\_rectangle(x1, y1,\\x2, y2, fill="blue")};

% Oval
\node[font=\small\bfseries] at (7,6.5) {Oval};
\draw[thick, green!60, fill=green!20] (6,5.25) ellipse (1 and 0.75);
\node[font=\tiny, align=left] at (7,4) {create\_oval(x1, y1, x2, y2,\\fill="green")};

% Polygon
\node[font=\small\bfseries] at (7,3) {Polygon};
\draw[thick, orange!80, fill=orange!20] (5.5,1.75) -- (6.5,2.5) -- (7.5,1.75) -- (7,1) -- (6,1) -- cycle;
\node[font=\tiny, align=left] at (7,0.2) {create\_polygon(x1, y1,\\x2, y2, ..., fill="yellow")};

% Arc
\node[font=\small\bfseries] at (12,6.5) {Arc};
\draw[thick, purple!80, fill=purple!20] (11,5.25) arc (180:0:1 and 0.75);
\draw[thick, purple!80] (11,5.25) -- (13,5.25);
\node[font=\tiny, align=left] at (12,4) {create\_arc(x1, y1, x2, y2,\\start=0, extent=180)};

% Text
\node[font=\small\bfseries] at (12,3) {Text};
\node[font=\normalsize, blue] at (12,2) {Hello!};
\node[font=\tiny, align=left] at (12,0.8) {create\_text(x, y,\\text="Hello!",\\font=("Arial", 16))};

\end{tikzpicture}
\end{center}

\subsubsection{Visualizzazione: Canvas Drawing Output}

\begin{tcolorbox}[title=Output Terminale - Canvas Drawing]
\begin{verbatim}
$ python3 canvas_demo.py

[Finestra GUI con Canvas]

Canvas Drawing:
+--------------------------------------+
|                                   /  |
| Line (rosso)                    /    |
|                               /      |
|   +---------+              /         |
|   | Rect    |   ( Oval )              |
|   | (blu)   |   (verde)               |
|   +---------+                         |
|                                       |
|    /\                                 |
|   /  \  Polygon (giallo)              |
|  /____\                               |
|                                       |
|           Hello Canvas! (testo)      |
+--------------------------------------+

Oggetti disegnati: 5 shape + 1 text
Canvas size: 400x300 pixels
\end{verbatim}
\end{tcolorbox}

\section{Threading per Operazioni Lunghe}

\begin{lstlisting}
import tkinter as tk
import threading
import time

def longTask():
    """Simula operazione lunga"""
    button.config(state=tk.DISABLED, text="Working...")

    def worker():
        time.sleep(3)  # Simula lavoro
        # Update UI dal main thread
        root.after(0, task_completed)

    thread = threading.Thread(target=worker)
    thread.start()

def taskCompleted():
    button.config(state=tk.NORMAL, text="Start Task")
    label.config(text="Task completed!")

root = tk.Tk()

label = tk.Label(root, text="Ready")
label.pack()

button = tk.Button(root, text="Start Task", command=long_task)
button.pack()

root.mainloop()
\end{lstlisting}

\section{Progetto: Calculator}

\begin{lstlisting}
import tkinter as tk

class Calculator:
    def __init__(self, root):
        self.root = root
        self.root.title("Calculator")

        self.expression = ""

        # Display
        self.display = tk.Entry(root, font=("Arial", 20), justify=tk.RIGHT)
        self.display.grid(row=0, column=0, columnspan=4, sticky="ew", padx=5, pady=5)

        # Buttons
        buttons = [
            ('7', 1, 0), ('8', 1, 1), ('9', 1, 2), ('/', 1, 3),
            ('4', 2, 0), ('5', 2, 1), ('6', 2, 2), ('*', 2, 3),
            ('1', 3, 0), ('2', 3, 1), ('3', 3, 2), ('-', 3, 3),
            ('0', 4, 0), ('.', 4, 1), ('+', 4, 2), ('=', 4, 3),
        ]

        for (text, row, col) in buttons:
            button = tk.Button(
                root,
                text=text,
                font=("Arial", 16),
                command=lambda t=text: self.onButtonClick(t)
            )
            button.grid(row=row, column=col, sticky="nsew", padx=2, pady=2)

        # Clear button
        tk.Button(
            root,
            text="C",
            font=("Arial", 16),
            command=self.clear
        ).grid(row=4, column=3, sticky="nsew", padx=2, pady=2)

        # Configure grid weights
        for i in range(5):
            root.grid_rowconfigure(i, weight=1)
        for i in range(4):
            root.grid_columnconfigure(i, weight=1)

    def onButtonClick(self, char):
        if char == '=':
            try:
                result = eval(self.expression)
                self.display.delete(0, tk.END)
                self.display.insert(0, str(result))
                self.expression = str(result)
            except:
                self.display.delete(0, tk.END)
                self.display.insert(0, "Error")
                self.expression = ""
        else:
            self.expression += char
            self.display.delete(0, tk.END)
            self.display.insert(0, self.expression)

    def clear(self):
        self.expression = ""
        self.display.delete(0, tk.END)

root = tk.Tk()
app = Calculator(root)
root.mainloop()
\end{lstlisting}

\begin{tcolorbox}[title=Best Practices]
La separazione tra logica di business e interfaccia utente attraverso pattern come MVC o MVP è fondamentale per mantenere il codice testabile e manutenibile. La UI dovrebbe occuparsi solo di presentazione e interazione, delegando la logica a moduli separati.

Un aspetto critico è evitare di bloccare il main thread con operazioni lunghe. Usare threading per I/O e operazioni computazionalmente costose mantiene l'interfaccia reattiva e previene il freeze dell'applicazione.

Per un aspetto moderno e nativo, i widget ttk dovrebbero essere preferiti ai widget classici di Tkinter, offrendo un look più professionale e compatibile con i temi del sistema operativo.

La validazione dell'input prima di processarlo è essenziale per prevenire errori e offrire feedback immediato all'utente. Gestire esplicitamente le eccezioni nelle callback previene crash silenziosi e permette di mostrare messaggi di errore significativi.

L'uso di variabili Tkinter come StringVar e IntVar facilita il binding bidirezionale tra widget e dati, semplificando l'aggiornamento dell'interfaccia quando i dati cambiano.

Per applicazioni complesse, organizzare il codice in classi rende la struttura più chiara e facilita il riuso di componenti. La scelta del layout manager è importante: grid è ideale per form e layout strutturati, mentre pack è più adatto per layout semplici e lineari. È fondamentale non mescolare pack e grid nello stesso container parent, poiché questo causa comportamenti imprevedibili.

Infine, implementare proper cleanup alla chiusura dell'applicazione (chiudere file, terminare thread) previene resource leak e garantisce che l'applicazione termini correttamente.
\end{tcolorbox}

\begin{tcolorbox}[title=Errori Comuni]
Uno degli errori più comuni è bloccare il main thread con operazioni lunghe, causando il freeze dell'interfaccia e una pessima esperienza utente. Operazioni I/O e calcoli pesanti devono sempre essere eseguiti in thread separati.

Mescolare pack e grid nello stesso parent container è un errore che causa comportamenti imprevedibili nel layout. I due layout manager non possono coesistere nello stesso contenitore.

Non gestire le eccezioni nelle callback porta a crash silenziosi difficili da debuggare. Ogni callback dovrebbe avere proper error handling per catturare e gestire eccezioni.

L'uso di \verb|eval()| senza validazione rappresenta un grave rischio di sicurezza, permettendo l'esecuzione di codice arbitrario. Input dell'utente non deve mai essere passato direttamente a eval.

Dimenticare di chiamare \verb|mainloop()| è un errore da principianti che impedisce all'applicazione di avviarsi, poiché senza il main loop non c'è gestione degli eventi.

Creare multiple istanze di \verb|Tk()| è quasi sempre un errore: un'applicazione Tkinter dovrebbe avere un solo root window. Per finestre secondarie, usare \verb|Toplevel()|.

Non configurare i grid weights porta a problemi di resize: le finestre non si ridimensionano correttamente perché le righe e colonne non sono configurate per espandersi.

Non usare ttk widgets rende l'applicazione visivamente datata, con un look old-school che non si integra bene con i sistemi operativi moderni.

Un errore critico è tentare di aggiornare l'UI da thread secondari, che causa crash e corruzione dello stato. Solo il main thread può manipolare widget; usare \verb|root.after()| per delegare update al main thread.

Infine, i memory leak possono derivare dal non fare cleanup dei widget rimossi dinamicamente. Widget non più usati dovrebbero essere esplicitamente distrutti con \verb|destroy()| per liberare risorse.
\end{tcolorbox}

\section{Esercizi}

\subsection{Livello Base}
\begin{enumerate}
  \item Crea form login (username, password, button)
  \item Implementa converter temperatura (Celsius $\leftrightarrow$ Fahrenheit)
  \item Todo list (add, display, clear)
\end{enumerate}

\subsection{Livello Intermedio}
\begin{enumerate}
  \item Text editor con menu (New, Open, Save, Exit)
  \item Calculator completa con history
  \item Image viewer con file dialog
\end{enumerate}

\subsection{Livello Avanzato}
\begin{enumerate}
  \item Drawing app con Canvas (brush, colors, clear)
  \item Database browser (SQLite) con CRUD operations
  \item File manager con tree view e operations
\end{enumerate}

\section{Riferimenti}
\begin{itemize}
  \item Tkinter Documentation: \url{https://docs.python.org/3/library/tk.html}
  \item Tkinter Tutorial: \url{https://realpython.com/python-gui-tkinter/}
  \item ttk widgets: \url{https://docs.python.org/3/library/tkinter.ttk.html}
\end{itemize}

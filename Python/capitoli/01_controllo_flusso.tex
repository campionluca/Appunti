% Modulo 01 — Controllo del Flusso
\chapter{Controllo del Flusso}
\label{chap:controllo-flusso}

\section{Introduzione}
\label{sec:cf-introduzione}
Questo modulo copre le strutture di controllo fondamentali in Python: condizioni, cicli e controllo del flusso dell'esecuzione.

La descrizione mette in evidenza quando preferire costrutti semplici e leggibili, come ridurre la complessità dei rami condizionali e come evitare loop inefficienti. Vedremo pattern comuni (guard clauses, early return) e insidie tipiche (condizioni annidate, off-by-one).

Capire bene il controllo del flusso è essenziale per scrivere codice chiaro, testabile e performante in scenari come elaborazione di liste, validazione di input e automazione di compiti ripetitivi.

\section{Obiettivi di Apprendimento}
\label{sec:cf-obiettivi}
\begin{itemize}
  \item Utilizzare \texttt{if}, \texttt{elif}, \texttt{else} con operatori di confronto e logici.
  \item Scrivere cicli \texttt{for} e \texttt{while}, usare \texttt{break}, \texttt{continue}, \texttt{pass}.
  \item Iterare su sequenze e range in modo idiomatico.
\end{itemize}

\section{Concetti Fondamentali}
\label{sec:cf-concetti}
\begin{tcolorbox}[title=Operatori]
Confronto: \texttt{==}, \texttt{!=}, \texttt{<}, \texttt{>}, \texttt{<=}, \texttt{>=}. Logici: \texttt{and}, \texttt{or}, \texttt{not}. Appartenenza: \texttt{in}, \texttt{not in}.
\end{tcolorbox}

\begin{tcolorbox}[title=Precedenza e short-circuit]
Gli operatori logici valutano da sinistra a destra e possono interrompere la valutazione (\emph{short-circuit}).
Usa parentesi per evitare ambiguità e preferisci condizioni semplici con guard clauses ed \emph{early return} per migliorare leggibilità.
\end{tcolorbox}

\subsubsection{Visualizzazione: Short-circuit evaluation}
\begin{center}
\begin{tikzpicture}[
    node distance=1.5cm,
    decision/.style={diamond, draw, fill=yellow!20, text width=2.5cm, text centered, inner sep=2pt},
    block/.style={rectangle, draw, fill=blue!20, text width=2.5cm, text centered, rounded corners, minimum height=0.8cm},
    result/.style={rectangle, draw, fill=green!20, text width=2.5cm, text centered, rounded corners, minimum height=0.8cm},
    shortcircuit/.style={rectangle, draw, fill=red!20, text width=2.5cm, text centered, rounded corners, minimum height=0.8cm},
    arrow/.style={thick,->,>=stealth}
]

% AND operator
\node (and_title) [above] at (0,0) {\textbf{Operatore AND}};
\node (and_cond1) [decision, below of=and_title, yshift=-0.5cm] {Condizione 1};
\node (and_short) [shortcircuit, below of=and_cond1, yshift=-0.5cm] {Risultato: False (short-circuit)};
\node (and_cond2) [decision, right of=and_cond1, xshift=3cm] {Condizione 2};
\node (and_result) [result, below of=and_cond2, yshift=-0.5cm] {Risultato finale};

\draw [arrow] (and_cond1) -- node[right] {False} (and_short);
\draw [arrow] (and_cond1) -- node[above] {True} (and_cond2);
\draw [arrow] (and_cond2) -- (and_result);

% OR operator
\node (or_title) [above] at (10,0) {\textbf{Operatore OR}};
\node (or_cond1) [decision, below of=or_title, yshift=-0.5cm] {Condizione 1};
\node (or_short) [shortcircuit, below of=or_cond1, yshift=-0.5cm] {Risultato: True (short-circuit)};
\node (or_cond2) [decision, right of=or_cond1, xshift=3cm] {Condizione 2};
\node (or_result) [result, below of=or_cond2, yshift=-0.5cm] {Risultato finale};

\draw [arrow] (or_cond1) -- node[right] {True} (or_short);
\draw [arrow] (or_cond1) -- node[above] {False} (or_cond2);
\draw [arrow] (or_cond2) -- (or_result);

\end{tikzpicture}
\end{center}

\begin{tcolorbox}[
    title=Confronto: Short-circuit in azione,
    colback=gray!5,
    colframe=gray!60,
    fonttitle=\bfseries,
    boxrule=0.8pt,
    arc=2pt,
    breakable=false
]

\begin{minipage}[t]{0.48\textwidth}
\begin{tcolorbox}[
    title=\textbf{Caso 1: Valuta entrambe},
    colback=orange!10,
    colframe=orange!80!black,
    fonttitle=\bfseries,
    boxrule=1.5pt,
    arc=2mm
]
\begin{lstlisting}
def check(x):
    print(f"Check {x}")
    return x > 5

a = 10
if check(a) and check(a+1):
    print("OK")

# Output:
# Check 10   -> True
# Check 11   -> True
# OK
\end{lstlisting}
\vspace{1mm}
\scriptsize\textbf{Risultato:} Prima \texttt{True} $\rightarrow$ valuta anche la seconda.
\end{tcolorbox}
\end{minipage}
\hfill
\begin{minipage}[t]{0.48\textwidth}
\begin{tcolorbox}[
    title=\textbf{Caso 2: Short-circuit!},
    colback=green!10,
    colframe=green!70!black,
    fonttitle=\bfseries,
    boxrule=1.5pt,
    arc=2mm
]
\begin{lstlisting}
def check(x):
    print(f"Check {x}")
    return x > 5

a = 3
if check(a) and check(a+1):
    print("OK")

# Output:
# Check 3    -> False
# (check(4) NON eseguito!)
\end{lstlisting}
\vspace{1mm}
\scriptsize\textbf{Risultato:} Prima \texttt{False} $\rightarrow$ interrompe!
\end{tcolorbox}
\end{minipage}

\end{tcolorbox}

\vspace{0.3cm}

\section{Esempi Pratici}
\label{sec:cf-esempi}
\subsection{Condizioni}
\label{subsec:cf-condizioni}
\begin{lstlisting}
eta = 18
if eta < 18:
    print("Minorenne")
elif eta == 18:
    print("Appena maggiorenne")
else:
    print("Maggiorenne")
\end{lstlisting}

\subsubsection{Visualizzazione: Flusso di controllo if/elif/else}
\begin{center}
\begin{tikzpicture}[
    node distance=1.5cm,
    decision/.style={diamond, draw, fill=yellow!20, text width=3cm, text centered, inner sep=2pt},
    block/.style={rectangle, draw, fill=green!20, text width=3cm, text centered, rounded corners, minimum height=1cm},
    output/.style={rectangle, draw, fill=blue!20, text width=3cm, text centered, rounded corners, minimum height=0.8cm},
    arrow/.style={thick,->,>=stealth}
]

% Nodi
\node (start) [block] {Leggi \texttt{eta = 18}};
\node (if) [decision, below of=start] {$eta < 18$?};
\node (elif) [decision, below of=if, yshift=-0.5cm] {$eta == 18$?};
\node (else) [output, below of=elif, yshift=-0.5cm] {"Maggiorenne"};
\node (output1) [output, right of=if, xshift=3cm] {"Minorenne"};
\node (output2) [output, right of=elif, xshift=3cm] {"Appena maggiorenne"};
\node (end) [block, below of=else] {Fine};

% Frecce
\draw [arrow] (start) -- (if);
\draw [arrow] (if) -- node[right] {No} (elif);
\draw [arrow] (if) -- node[above] {Sì} (output1);
\draw [arrow] (elif) -- node[above] {Sì} (output2);
\draw [arrow] (elif) -- node[right] {No} (else);
\draw [arrow] (output1) |- (end);
\draw [arrow] (output2) |- (end);
\draw [arrow] (else) -- (end);

\end{tikzpicture}
\end{center}

\begin{tcolorbox}[title=Terminal Output]
\begin{verbatim}
$ python condizioni.py
Appena maggiorenne
\end{verbatim}
\end{tcolorbox}

\begin{tcolorbox}[title=Spiegazione del codice]
\begin{itemize}
  \item \textbf{Branching (ramificazione)}: Le catene \texttt{if/elif/else} gestiscono casi mutuamente esclusivi. Quando una condizione è vera, Python esegue quel blocco e salta tutti gli altri. Nel nostro esempio, se \texttt{eta < 18}, Python stampa "Minorenne" e ignora \texttt{elif} ed \texttt{else}.
  \item \textbf{Separare logica da output}: Nell'esempio sopra, la stampa è l'effetto collaterale. È meglio separare la decisione (logica) dalla presentazione (I/O). Esempio migliorato:
\end{itemize}
\end{tcolorbox}

\begin{lstlisting}
# Migliore: logica separata dall'output
def classificaEta(eta):
    if eta < 18:
        return "minorenne"
    elif eta == 18:
        return "appenaMaggiorenne"
    else:
        return "maggiorenne"

# La funzione restituisce dati, non stampa
categoria = classificaEta(18)
print(f"Categoria: {categoria}")  # Output gestito separatamente
\end{lstlisting}
\begin{tcolorbox}[title=Perché separare logica da I/O?]
\begin{itemize}
  \item \textbf{Testabilità}: Puoi testare \texttt{classificaEta()} senza dover catturare l'output della console
  \item \textbf{Riusabilità}: La funzione può essere usata sia in un programma console che in un'interfaccia grafica
  \item \textbf{Flessibilità}: Puoi cambiare il formato dell'output senza modificare la logica
\end{itemize}
\end{tcolorbox}

\subsection{Early Return}
\label{subsec:cf-early-return}

\begin{tcolorbox}[
    title=\textbf{Senza early return - Annidamento profondo},
    colback=red!10,
    colframe=red!80!black,
    fonttitle=\bfseries,
    boxrule=1.5pt,
    arc=2mm
]
\begin{lstlisting}
def calcolaSconto(eta, eMembro, importo):
    if eta >= 18:
        if eMembro:
            if importo > 100:
                return importo * 0.2
            else:
                return importo * 0.1
        else:
            return 0
    else:
        return 0

# Utilizzo
print(calcolaSconto(25, True, 150))
# Output: 30.0
\end{lstlisting}
\textbf{Problema:} 3 livelli di annidamento rendono difficile seguire la logica. Devi leggere tutto il codice per capire quando ritorna 0.
\end{tcolorbox}

\vspace{0.3cm}

\begin{tcolorbox}[
    title=\textbf{Con early return (guard clauses)},
    colback=green!10,
    colframe=green!70!black,
    fonttitle=\bfseries,
    boxrule=1.5pt,
    arc=2mm
]
\begin{lstlisting}
def calcolaScontoV2(eta, eMembro, importo):
    if eta < 18:
        return 0
    if not eMembro:
        return 0
    if importo > 100:
        return importo * 0.2
    return importo * 0.1

# Utilizzo
print(calcolaScontoV2(25, True, 150))
# Output: 30.0
\end{lstlisting}
\textbf{Vantaggio:} Logica lineare e chiara. I casi speciali (minorenne, non membro) sono gestiti subito all'inizio con un return immediato. Il codice è più facile da leggere dall'alto verso il basso.
\end{tcolorbox}

\begin{tcolorbox}[title=Guard Clauses]
Le \textbf{guard clauses} sono controlli che escludono subito i casi non validi o speciali, permettendo al codice principale di procedere senza annidamenti. Questo pattern:
\begin{itemize}
  \item Riduce la complessità cognitiva
  \item Rende il codice più manutenibile
  \item Separa chiaramente i casi speciali dalla logica principale
\end{itemize}
\end{tcolorbox}

\vspace{0.3cm}

\subsection{Cicli for e while}
\label{subsec:cf-cicli}

\subsubsection{La funzione range()}
\label{subsubsec:cf-range}
\begin{lstlisting}
# range(stop): genera numeri da 0 a stop-1
for i in range(5):
    print(i)
# Output: 0, 1, 2, 3, 4

# range(start, stop): genera numeri da start a stop-1
for i in range(2, 6):
    print(i)
# Output: 2, 3, 4, 5

# range(start, stop, step): genera numeri con incremento personalizzato
for i in range(0, 10, 2):
    print(i)
# Output: 0, 2, 4, 6, 8

# range con step negativo: contare all'indietro
for i in range(5, 0, -1):
    print(i)
# Output: 5, 4, 3, 2, 1
\end{lstlisting}

\begin{tcolorbox}[title=Come funziona range()]
\texttt{range()} genera una sequenza di numeri interi. Accetta 1, 2 o 3 argomenti:
\begin{itemize}
  \item \texttt{range(stop)}: genera da 0 a stop-1
  \item \texttt{range(start, stop)}: genera da start a stop-1
  \item \texttt{range(start, stop, step)}: genera da start a stop-1, incrementando di step
\end{itemize}
\textbf{Importante}: Il valore \texttt{stop} è sempre escluso dalla sequenza.
\end{tcolorbox}

\begin{tcolorbox}[title=Terminal Output - Esempi range()]
\begin{verbatim}
$ python range_examples.py

# range(5)
0
1
2
3
4

# range(2, 6)
2
3
4
5

# range(0, 10, 2)
0
2
4
6
8

# range(5, 0, -1)
5
4
3
2
1
\end{verbatim}
\end{tcolorbox}

\subsubsection{enumerate() per collezioni}
\label{subsubsec:cf-enumerate}
\begin{lstlisting}
# Modo sbagliato: usare range() e accedere per indice
nomi = ["Alice", "Bob", "Charlie"]
for i in range(len(nomi)):
    print(f"{i}: {nomi[i]}")

# Modo migliore: usare enumerate()
nomi = ["Alice", "Bob", "Charlie"]
for indice, nome in enumerate(nomi):
    print(f"{indice}: {nome}")

# enumerate() con indice iniziale personalizzato
for num, nome in enumerate(nomi, start=1):
    print(f"{num}. {nome}")
# Output:
# 1. Alice
# 2. Bob
# 3. Charlie
\end{lstlisting}

\begin{tcolorbox}[title=Perché usare enumerate()]
\texttt{enumerate()} è preferibile a \texttt{range(len(...))} perché:
\begin{itemize}
  \item Più leggibile: ottieni direttamente indice e valore
  \item Più sicuro: eviti errori di accesso fuori range
  \item Più Pythonic: segue le convenzioni del linguaggio
\end{itemize}
\end{tcolorbox}

\begin{tcolorbox}[title=Terminal Output - enumerate()]
\begin{verbatim}
$ python enumerate_example.py

# Con enumerate(nomi)
0: Alice
1: Bob
2: Charlie

# Con enumerate(nomi, start=1)
1. Alice
2. Bob
3. Charlie
\end{verbatim}
\end{tcolorbox}

\subsubsection{Ciclo while}
\label{subsubsec:cf-while}
\begin{lstlisting}
# while con condizione
count = 0
while count < 3:
    print(count)
    count += 1
# Output: 0, 1, 2
\end{lstlisting}
\begin{tcolorbox}[title=Quando usare while vs for]
\begin{itemize}
  \item \textbf{for}: quando sai quante iterazioni fare o stai iterando su una collezione
  \item \textbf{while}: quando non sai in anticipo quante iterazioni servono (es: input utente, condizioni complesse)
  \item \textbf{Evita}: \texttt{while True} con break se puoi usare una condizione esplicita
\end{itemize}
\end{tcolorbox}

\subsubsection{Visualizzazione: Confronto for vs while}
\begin{center}
\begin{tikzpicture}[
    node distance=1.5cm,
    decision/.style={diamond, draw, fill=yellow!20, text width=2.5cm, text centered, inner sep=2pt},
    block/.style={rectangle, draw, fill=blue!20, text width=3cm, text centered, rounded corners, minimum height=0.8cm},
    process/.style={rectangle, draw, fill=green!20, text width=3cm, text centered, rounded corners, minimum height=0.8cm},
    arrow/.style={thick,->,>=stealth}
]

% FOR LOOP
\node (for_title) [above] at (0,0) {\textbf{Ciclo FOR}};
\node (for_init) [block, below of=for_title, yshift=-0.5cm] {Inizializza iteratore su collezione};
\node (for_check) [decision, below of=for_init] {Elementi rimanenti?};
\node (for_body) [process, below of=for_check, yshift=-0.5cm] {Esegui corpo del ciclo};
\node (for_next) [block, below of=for_body] {Elemento successivo};
\node (for_end) [block, below of=for_check, xshift=-3.5cm, yshift=-2cm] {Fine};

\draw [arrow] (for_init) -- (for_check);
\draw [arrow] (for_check) -- node[right] {Sì} (for_body);
\draw [arrow] (for_check) -- node[above] {No} (for_end);
\draw [arrow] (for_body) -- (for_next);
\draw [arrow] (for_next) -| (for_check);

% WHILE LOOP
\node (while_title) [above] at (9,0) {\textbf{Ciclo WHILE}};
\node (while_init) [block, below of=while_title, yshift=-0.5cm] {Inizializza variabili};
\node (while_check) [decision, below of=while_init] {Condizione vera?};
\node (while_body) [process, below of=while_check, yshift=-0.5cm] {Esegui corpo del ciclo};
\node (while_update) [block, below of=while_body] {Aggiorna variabili};
\node (while_end) [block, below of=while_check, xshift=-3.5cm, yshift=-2cm] {Fine};

\draw [arrow] (while_init) -- (while_check);
\draw [arrow] (while_check) -- node[right] {Sì} (while_body);
\draw [arrow] (while_check) -- node[above] {No} (while_end);
\draw [arrow] (while_body) -- (while_update);
\draw [arrow] (while_update) -| (while_check);

\end{tikzpicture}
\end{center}

\begin{tcolorbox}[title=Terminal Output - Confronto for vs while]
\begin{verbatim}
$ python loop_comparison.py

# FOR loop con range(3)
Iterazione for: 0
Iterazione for: 1
Iterazione for: 2

# WHILE loop equivalente
Iterazione while: 0
Iterazione while: 1
Iterazione while: 2
\end{verbatim}
\end{tcolorbox}

\subsection{break e continue}
\label{subsec:cf-break-continue}
\begin{lstlisting}
for n in range(10):
    if n == 5:
        break
    if n % 2 == 0:
        continue
    print(n)  # stampa solo dispari prima del 5
\end{lstlisting}
\begin{tcolorbox}[title=Spiegazione del codice]
\begin{itemize}
  \item \textbf{break}: interrompe il ciclo quando `n == 5`; utile per early exit.
  \item \textbf{continue}: salta iterazioni per numeri pari; riduce annidamento.
  \item \textbf{Intento}: il commento esplicita comportamento; preferire condizioni leggibili a logica implicita.
\end{itemize}
\end{tcolorbox}

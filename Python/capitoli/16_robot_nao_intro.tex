% Modulo 16 - Introduzione a NAO (V6) e NAOqi
\chapter{Introduzione a NAO (V6) e NAOqi}

\section{Contesto e Applicazioni}
\begin{tcolorbox}[title=Contesto e Applicazioni]
\begin{itemize}
  \item Didattica STEM e laboratori universitari.
  \item Social robotics, interazione uomo-robot (HRI).
  \item Demo e prototipi per mostre ed eventi.
  \item Sperimentazione di visione, linguaggio e movimento.
\end{itemize}
\end{tcolorbox}

\section{Approfondimenti}
NAO (V6) utilizza NAOqi 2.x: un framework di servizi (\emph{AL\*}) accessibili via rete. La programmazione in Python avviene tipicamente creando una sessione (\texttt{qi}) e recuperando i servizi, come \texttt{ALTextToSpeech}, \texttt{ALMotion}, \texttt{ALRobotPosture}, \texttt{ALVideoDevice}.

\subsubsection{Visualizzazione: Anatomia del Robot NAO}
Il robot NAO V6 dispone di 25 gradi di libertà distribuiti su testa, braccia, gambe e torso. La seguente figura mostra le principali articolazioni e sensori.

\begin{center}
\begin{tikzpicture}[scale=1.2, every node/.style={font=\small}]
% Head
\draw[fill=blue!20, draw=black, thick] (0,8) circle (0.6);
\node at (0,8) {\textbf{Testa}};
\draw[->, thick, red] (0.6,8.3) -- (1.5,8.8) node[right] {Camera superiore};
\draw[->, thick, red] (0.6,7.7) -- (1.5,7.2) node[right] {Camera inferiore};
\node at (0,9) {\small HeadYaw, HeadPitch};

% Torso
\draw[fill=gray!20, draw=black, thick, rounded corners] (-0.8,6.5) rectangle (0.8,7.5);
\node at (0,7) {\textbf{Torso}};

% Left Arm
\draw[fill=green!20, draw=black, thick] (-0.8,7) -- (-2,6.5) -- (-2,5) -- (-1.5,5) -- (-1.5,6.5) -- cycle;
\node at (-1.7,5.7) [rotate=0, align=center] {\tiny Braccio\\[-1mm]\tiny Sinistro};
\draw[->, thick, blue] (-2.2,6.2) -- (-3.2,6.2) node[left, align=right] {\tiny LShoulderPitch\\[-1mm]\tiny LShoulderRoll};
\draw[->, thick, blue] (-2.2,5.5) -- (-3.2,5.5) node[left, align=right] {\tiny LElbowYaw\\[-1mm]\tiny LElbowRoll};

% Right Arm
\draw[fill=green!20, draw=black, thick] (0.8,7) -- (2,6.5) -- (2,5) -- (1.5,5) -- (1.5,6.5) -- cycle;
\node at (1.7,5.7) [rotate=0, align=center] {\tiny Braccio\\[-1mm]\tiny Destro};
\draw[->, thick, blue] (2.2,6.2) -- (3.2,6.2) node[right, align=left] {\tiny RShoulderPitch\\[-1mm]\tiny RShoulderRoll};
\draw[->, thick, blue] (2.2,5.5) -- (3.2,5.5) node[right, align=left] {\tiny RElbowYaw\\[-1mm]\tiny RElbowRoll};

% Hands
\draw[fill=orange!20, draw=black] (-2,4.8) circle (0.2);
\node at (-2,4.3) {\tiny LHand};
\draw[fill=orange!20, draw=black] (2,4.8) circle (0.2);
\node at (2,4.3) {\tiny RHand};

% Pelvis
\draw[fill=gray!30, draw=black, thick] (-0.8,5) rectangle (0.8,6.5);
\node at (0,5.7) {\small HipPitch};

% Left Leg
\draw[fill=yellow!20, draw=black, thick] (-0.6,5) -- (-0.6,3) -- (-1,3) -- (-1,1) -- (-0.5,1) -- (-0.5,5) -- cycle;
\node at (-0.75,3.5) [rotate=90, align=center] {\tiny Gamba Sx};
\draw[->, thick, purple] (-1.2,4) -- (-2,4) node[left, align=right] {\tiny LHipYawPitch\\[-1mm]\tiny LHipRoll\\[-1mm]\tiny LHipPitch};
\draw[->, thick, purple] (-1.2,2) -- (-2,2) node[left, align=right] {\tiny LKneePitch};

% Right Leg
\draw[fill=yellow!20, draw=black, thick] (0.6,5) -- (0.6,3) -- (1,3) -- (1,1) -- (0.5,1) -- (0.5,5) -- cycle;
\node at (0.75,3.5) [rotate=90, align=center] {\tiny Gamba Dx};
\draw[->, thick, purple] (1.2,4) -- (2,4) node[right, align=left] {\tiny RHipYawPitch\\[-1mm]\tiny RHipRoll\\[-1mm]\tiny RHipPitch};
\draw[->, thick, purple] (1.2,2) -- (2,2) node[right, align=left] {\tiny RKneePitch};

% Feet
\draw[fill=brown!30, draw=black, thick] (-1.2,0.5) rectangle (-0.3,1);
\node at (-0.75,0.2) {\tiny Piede Sx};
\draw[->, thick, orange] (-1.2,0.7) -- (-2,0.7) node[left, align=right] {\tiny LAnklePitch\\[-1mm]\tiny LAnkleRoll};

\draw[fill=brown!30, draw=black, thick] (0.3,0.5) rectangle (1.2,1);
\node at (0.75,0.2) {\tiny Piede Dx};
\draw[->, thick, orange] (1.2,0.7) -- (2,0.7) node[right, align=left] {\tiny RAnklePitch\\[-1mm]\tiny RAnkleRoll};

% Legend
\draw[fill=white, draw=black] (-4,-0.5) rectangle (4,-1.5);
\node at (0,-0.7) [align=center] {\textbf{Legenda Catene Cinematiche}};
\node at (-2,-1.1) [align=left, font=\tiny] {Testa: 2 DOF | Braccia: 4+4 DOF | Mani: 2 DOF};
\node at (2,-1.1) [align=left, font=\tiny] {Gambe: 5+5 DOF | Piedi: sensori di pressione};

\end{tikzpicture}
\end{center}

\subsubsection{Visualizzazione: Architettura NAOqi Services}
NAOqi è organizzato come un sistema di servizi modulari comunicanti via broker.

\begin{center}
\begin{tikzpicture}[
    service/.style={rectangle, draw=blue!60, fill=blue!10, thick, minimum width=2.5cm, minimum height=0.8cm, rounded corners},
    broker/.style={rectangle, draw=red!60, fill=red!10, thick, minimum width=3cm, minimum height=1cm},
    client/.style={rectangle, draw=green!60, fill=green!10, thick, minimum width=2.5cm, minimum height=0.8cm},
    arrow/.style={->, thick, >=stealth}
]

% NAOqi Broker
\node[broker] (broker) at (0,0) {\textbf{NAOqi Broker}};

% Services
\node[service] (motion) at (-3,2.5) {ALMotion};
\node[service] (tts) at (0,2.5) {ALTextToSpeech};
\node[service] (video) at (3,2.5) {ALVideoDevice};
\node[service] (posture) at (-3,-2.5) {ALRobotPosture};
\node[service] (audio) at (0,-2.5) {ALAudioDevice};
\node[service] (memory) at (3,-2.5) {ALMemory};

% Client
\node[client] (client) at (-6,0) {Python Client\\(qi.Session)};

% Arrows from services to broker
\draw[arrow] (motion) -- (broker);
\draw[arrow] (tts) -- (broker);
\draw[arrow] (video) -- (broker);
\draw[arrow] (posture) -- (broker);
\draw[arrow] (audio) -- (broker);
\draw[arrow] (memory) -- (broker);

% Arrow from client to broker
\draw[arrow, very thick, blue] (client) -- node[above] {TCP 9559} (broker);

% Network annotation
\node at (-6,-1.5) [align=center, font=\small] {Connessione\\remota o locale};

\end{tikzpicture}
\end{center}

Il client Python utilizza \texttt{qi.Session} per connettersi al broker NAOqi (porta 9559) e richiedere i servizi necessari. Ogni servizio espone metodi invocabili via RPC.

\begin{tcolorbox}[title=Prime connessioni]
\begin{lstlisting}
# Connessione ai servizi NAOqi (NAO V6)
import qi

session = qi.Session()
session.connect("tcp://nao.local:9559")  # sostituisci host/IP

tts = session.service("ALTextToSpeech")
tts.say("Ciao! Sono NAO")

motion = session.service("ALMotion")
posture = session.service("ALRobotPosture")
# Passa alla postura base prima di muovere
posture.goToPosture("StandInit", 0.5)
\end{lstlisting}
\end{tcolorbox}

\paragraph{Note pratiche} Assicurare connettività (Wi-Fi/Ethernet) e versione corretta del SDK. Per stabilità, gestire eccezioni di rete e latenza; loggare operazioni critiche.

\begin{tcolorbox}[title=Guida rapida per programmatori non-Python]
Python è tipizzato dinamicamente e l'indentazione fa parte della sintassi (blocchi). Le eccezioni sono il meccanismo standard di errore; non si usano codici di ritorno come in C. Le liste (array dinamici) e i dizionari (mappe chiave->valore) sono strutture fondamentali.

\begin{itemize}
  \item Import: \texttt{import qi} carica il modulo del SDK NAOqi.
  \item Sessione: \texttt{qi.Session()} crea un oggetto che gestisce la connessione TCP verso il robot.
  \item Servizi: \texttt{session.service("ALMotion")} risolve il servizio remoto e ritorna un proxy su cui invocare metodi.
\end{itemize}

\begin{itemize}
  \item \textbf{Gestione errori}: usare \texttt{try/except} per catturare \texttt{RuntimeError} quando la connessione fallisce.
  \item \textbf{Stringhe}: sono Unicode; passare testo direttamente a \texttt{tts.say}.
  \item \textbf{Convenzione}: nomi come \texttt{goToPosture} sono in stile CamelCase perché ereditati dalle API NAOqi.
\end{itemize}
\end{tcolorbox}

\begin{tcolorbox}[title=Connessione commentata passo-passo]
\begin{lstlisting}
import qi

def connect(host="nao.local", port=9559):
    """Stabilisce una sessione TCP con NAO.
    - host: hostname o IP del robot
    - port: porta NAOqi (default 9559)
    Ritorna: qi.Session connessa.
    """
    session = qi.Session()
    try:
        session.connect(f"tcp://{host}:{port}")
    except RuntimeError as e:
        # In Python le eccezioni si catturano per tipo; qui gestiamo problemi di rete
        print("Connessione fallita:", e)  # Output (in caso di errore): Connessione fallita: [messaggio errore]
        raise
    return session

session = connect()
tts = session.service("ALTextToSpeech")  # Proxy al servizio TTS
motion = session.service("ALMotion")      # Proxy al servizio di movimento
posture = session.service("ALRobotPosture")

# Prima di muovere: posizionare NAO in postura stabile
posture.goToPosture("StandInit", 0.5)  # 0.5 = fattore di velocità (0..1)
tts.say("Pronto a muovermi!")
\end{lstlisting}
\end{tcolorbox}

\subsubsection{Visualizzazione: Esempio di Output Terminale}
Di seguito un esempio di interazione con il robot NAO da terminale, mostrando connessione e primo comando vocale.

\begin{tcolorbox}[title=Output Terminale - Prima Connessione]
\begin{verbatim}
$ python nao_test.py
[INFO ] Connecting to NAO at tcp://nao.local:9559...
[INFO ] Session connected successfully
[INFO ] Retrieved service: ALTextToSpeech
[INFO ] Retrieved service: ALMotion
[INFO ] Retrieved service: ALRobotPosture
[INFO ] Setting posture to StandInit...
[INFO ] NAO speaking: "Ciao! Sono NAO"
[INFO ] Posture reached
[SUCCESS] Script completed successfully
\end{verbatim}
\end{tcolorbox}


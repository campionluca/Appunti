% Modulo 15 — Database con SQLite e SQLAlchemy
\chapter{Database con SQLite e SQLAlchemy}

\begin{tcolorbox}[title=Obiettivi del capitolo]
Dopo questo capitolo saprai:
\begin{itemize}
  \item Comprendere database relazionali e SQL
  \item Usare SQLite3 per query e operazioni CRUD
  \item Definire modelli con SQLAlchemy ORM
  \item Gestire relationships (One-to-Many, Many-to-Many)
  \item Eseguire query complesse con SQLAlchemy
  \item Implementare transazioni e gestione errori
  \item Usare Alembic per migrazioni schema
  \item Evitare problemi comuni (N+1, SQL injection)
  \item Applicare best practices per performance e sicurezza
\end{itemize}
\end{tcolorbox}

\section{Introduzione}

I database sono una componente fondamentale di qualsiasi applicazione moderna, permettendo di risolvere problemi cruciali nello sviluppo software.

Dal punto di vista della \textbf{persistenza}, i database permettono di salvare dati in modo permanente, consentendo alle applicazioni di mantenere stato oltre il ciclo di esecuzione di un programma.

Una delle funzionalità principali è la capacità di \textbf{eseguire query}, cioè cercare e filtrare dati efficientemente. Anziché cariccare tutto in memoria e filtrare manualmente, i database usano indici e ottimizzatori di query per fornire risultati rapidamente.

L'\textbf{integrità dei dati} è assicurata da vincoli (constraints) come chiavi primarie, chiavi esterne e vincoli di unicità. Questi garantiscono consistenza e validità dei dati nel database.

La \textbf{concorrenza} è un aspetto critico spesso trascurato. I database moderni permettono di gestire accessi multipli simultanei in modo sicuro, usando mecanismi come locking e transazioni.

Infine, la \textbf{scalabilità} è una considerazione importante. Un database ben progettato e dotato di indici appropriati può gestire milioni di record mantenendo performance accettabili.

\subsection{SQLite vs altri database}

\textbf{SQLite} è un database file-based che memorizza tutti i dati in un singolo file \texttt{.db}, senza richiedere un server separato da configurare o gestire. Questa semplicità lo rende ideale per prototipi rapidi, applicazioni desktop, testing e sistemi embedded dove non è necessaria alta concorrenza. Tuttavia, presenta limitazioni significative in termini di concorrenza (scrive bloccanti) e non supporta clustering o distribuzione.

\textbf{PostgreSQL e MySQL} utilizzano un'architettura client-server che richiede l'installazione e configurazione di un server database separato. Questa architettura permette alta concorrenza con multiple connessioni simultanee e performance scalabili, rendendoli ideali per ambienti di produzione, applicazioni web e scenari che richiedono alta disponibilità e scalabilità orizzontale.

\subsection{SQL vs ORM}

L'approccio \textbf{Raw SQL} con sqlite3 offre controllo completo sulle query e massima performance, permettendo di ottimizzare ogni aspetto dell'interazione con il database. Tuttavia, richiede scrittura di codice più verboso e ripetitivo, con il rischio costante di SQL injection se non si usano correttamente i prepared statements.

L'approccio \textbf{ORM} con SQLAlchemy fornisce un'interfaccia Pythonic che astrae le query SQL in operazioni su oggetti, offrendo type safety e riducendo significativamente il boilerplate. La curva di apprendimento iniziale è più ripida e c'è un overhead di performance minimo, ma i benefici in termini di manutenibilità e produttività generalmente superano questi svantaggi.

\section{SQLite3: Database di Base}

\subsection{Connessione e creazione database}

\begin{lstlisting}
import sqlite3

# Database in memoria (temporaneo)
conn = sqlite3.connect(":memory:")

# Database su file (persistente)
conn = sqlite3.connect("mydatabase.db")

# Row factory per dict invece di tuple
conn.row_factory = sqlite3.Row

cursor = conn.cursor()

# SEMPRE chiudere connessione
try:
    # ... operazioni database ...
    pass
finally:
    conn.close()

# Oppure usa context manager (raccomandato)
with sqlite3.connect("mydatabase.db") as conn:
    cursor = conn.cursor()
    # Operazioni qui
    # Commit automatico se no eccezioni
\end{lstlisting}

\subsection{CREATE TABLE}

\begin{lstlisting}
import sqlite3

conn = sqlite3.connect("blog.db")
cursor = conn.cursor()

# Crea tabella users
cursor.execute("""
    CREATE TABLE IF NOT EXISTS users (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        username TEXT UNIQUE NOT NULL,
        email TEXT UNIQUE NOT NULL,
        password_hash TEXT NOT NULL,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    )
""")

# Crea tabella posts con foreign key
cursor.execute("""
    CREATE TABLE IF NOT EXISTS posts (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        user_id INTEGER NOT NULL,
        title TEXT NOT NULL,
        content TEXT NOT NULL,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (user_id) REFERENCES users (id)
            ON DELETE CASCADE
    )
""")

# Crea indici per performance
cursor.execute("CREATE INDEX IF NOT EXISTS idx_username ON users(username)")
cursor.execute("CREATE INDEX IF NOT EXISTS idx_user_id ON posts(user_id)")

conn.commit()
conn.close()
\end{lstlisting}

\subsubsection{Visualizzazione: Schema Database Blog}

\begin{center}
\begin{tikzpicture}[
  table/.style={rectangle, draw=blue!60, fill=blue!5, thick, minimum width=3.5cm, minimum height=0.8cm},
  field/.style={rectangle, draw=gray!60, fill=white, minimum width=3.5cm, minimum height=0.6cm, font=\small},
  pk/.style={fill=yellow!20},
  fk/.style={fill=green!10}
]

% Users table
\node[table] (users_table) at (0, 0) {\textbf{users}};
\node[field, pk, below=0cm of users_table] (users_id) {\texttt{id} INTEGER PK};
\node[field, below=0cm of users_id] (users_username) {\texttt{username} TEXT UNIQUE};
\node[field, below=0cm of users_username] (users_email) {\texttt{email} TEXT UNIQUE};
\node[field, below=0cm of users_email] (users_password) {\texttt{password\_hash} TEXT};
\node[field, below=0cm of users_password] (users_created) {\texttt{created\_at} TIMESTAMP};

% Posts table
\node[table] (posts_table) at (6, 0) {\textbf{posts}};
\node[field, pk, below=0cm of posts_table] (posts_id) {\texttt{id} INTEGER PK};
\node[field, fk, below=0cm of posts_id] (posts_user_id) {\texttt{user\_id} INTEGER FK};
\node[field, below=0cm of posts_user_id] (posts_title) {\texttt{title} TEXT};
\node[field, below=0cm of posts_title] (posts_content) {\texttt{content} TEXT};
\node[field, below=0cm of posts_content] (posts_created) {\texttt{created\_at} TIMESTAMP};

% Relationship
\draw[->, thick, blue!70] (posts_user_id.west) -- node[above, font=\small] {N:1} (users_id.east);

% Legend
\node[draw, fill=yellow!20, minimum width=0.5cm, minimum height=0.3cm] at (-2, -5) {};
\node[right, font=\small] at (-1.5, -5) {Primary Key (PK)};

\node[draw, fill=green!10, minimum width=0.5cm, minimum height=0.3cm] at (-2, -5.7) {};
\node[right, font=\small] at (-1.5, -5.7) {Foreign Key (FK)};

\end{tikzpicture}
\end{center}

\subsection{INSERT: Prepared Statements}

\begin{lstlisting}
import sqlite3

conn = sqlite3.connect("blog.db")
cursor = conn.cursor()

# INSICURO - SQL Injection vulnerability!
# username = input("Username: ")  # Se utente inserisce: ' OR '1'='1
# cursor.execute(f"INSERT INTO users (username) VALUES ('{username}')")

# SICURO - Prepared statements con ? placeholder
username = "alice"
email = "alice@example.com"
password_hash = "hashed_password"

cursor.execute(
    "INSERT INTO users (username, email, password_hash) VALUES (?, ?, ?)",
    (username, email, password_hash)
)

# Ottieni ID auto-incrementato
user_id = cursor.lastrowid
print(f"Inserted user with ID: {user_id}")

# INSERT multipli (più efficiente)
users = [
    ("bob", "bob@example.com", "hash1"),
    ("charlie", "charlie@example.com", "hash2"),
    ("diana", "diana@example.com", "hash3")
]

cursor.executemany(
    "INSERT INTO users (username, email, password_hash) VALUES (?, ?, ?)",
    users
)

conn.commit()
conn.close()
\end{lstlisting}

\subsubsection{Visualizzazione: Output Terminale INSERT}

\begin{tcolorbox}[colback=black!5, colframe=black!75, title=Esecuzione INSERT con SQLite3, fonttitle=\bfseries\ttfamily]
\begin{verbatim}
$ python insert_users.py
Inserted user with ID: 1

$ sqlite3 blog.db
SQLite version 3.39.5
sqlite> SELECT * FROM users;
id|username|email|password_hash|created_at
1|alice|alice@example.com|hashed_password|2024-01-15 10:30:45
2|bob|bob@example.com|hash1|2024-01-15 10:30:45
3|charlie|charlie@example.com|hash2|2024-01-15 10:30:45
4|diana|diana@example.com|hash3|2024-01-15 10:30:45
\end{verbatim}
\end{tcolorbox}

\subsection{SELECT: Query dati}

\begin{lstlisting}
import sqlite3

conn = sqlite3.connect("blog.db")
conn.row_factory = sqlite3.Row  # Row invece di tuple
cursor = conn.cursor()

# SELECT singolo
cursor.execute("SELECT * FROM users WHERE username = ?", ("alice",))
user = cursor.fetchone()

if user:
    print(f"ID: {user['id']}, Username: {user['username']}, Email: {user['email']}")
else:
    print("User not found")

# SELECT multipli
cursor.execute("SELECT * FROM users ORDER BY created_at DESC")
users = cursor.fetchall()

for user in users:
    print(f"{user['username']} - {user['email']}")

# SELECT con JOIN
cursor.execute("""
    SELECT users.username, posts.title, posts.created_at
    FROM posts
    JOIN users ON posts.user_id = users.id
    WHERE users.username = ?
    ORDER BY posts.created_at DESC
""", ("alice",))

posts = cursor.fetchall()
for post in posts:
    print(f"{post['username']}: {post['title']}")

# SELECT con aggregazione
cursor.execute("""
    SELECT users.username, COUNT(posts.id) as post_count
    FROM users
    LEFT JOIN posts ON users.id = posts.user_id
    GROUP BY users.id
    HAVING post_count > 0
""")

stats = cursor.fetchall()
for stat in stats:
    print(f"{stat['username']}: {stat['post_count']} posts")

conn.close()
\end{lstlisting}

\subsubsection{Visualizzazione: Tipi di JOIN in SQL}

\begin{center}
\begin{tikzpicture}[
  circle/.style={draw, circle, minimum size=2cm, thick},
  label/.style={font=\small\bfseries}
]

% INNER JOIN
\begin{scope}[shift={(0,0)}]
  \draw[fill=blue!30, draw=blue!60, thick] (0.5,0) circle (1cm);
  \draw[fill=red!30, draw=red!60, thick] (-0.5,0) circle (1cm);
  \begin{scope}
    \clip (0.5,0) circle (1cm);
    \fill[purple!50] (-0.5,0) circle (1cm);
  \end{scope}
  \node[label] at (0, -1.8) {INNER JOIN};
  \node[font=\tiny] at (-1.2, 0) {A};
  \node[font=\tiny] at (1.2, 0) {B};
  \node[font=\tiny] at (0, 0.5) {A $\cap$ B};
\end{scope}

% LEFT JOIN
\begin{scope}[shift={(5,0)}]
  \draw[fill=blue!50, draw=blue!60, thick] (0.5,0) circle (1cm);
  \draw[fill=red!30, draw=red!60, thick] (-0.5,0) circle (1cm);
  \begin{scope}
    \clip (0.5,0) circle (1cm);
    \fill[purple!50] (-0.5,0) circle (1cm);
  \end{scope}
  \node[label] at (0, -1.8) {LEFT JOIN};
  \node[font=\tiny] at (-1.2, 0) {A};
  \node[font=\tiny] at (1.2, 0) {B};
  \node[font=\tiny] at (-0.5, 0.5) {Tutti A};
\end{scope}

% RIGHT JOIN
\begin{scope}[shift={(10,0)}]
  \draw[fill=blue!30, draw=blue!60, thick] (0.5,0) circle (1cm);
  \draw[fill=red!50, draw=red!60, thick] (-0.5,0) circle (1cm);
  \begin{scope}
    \clip (0.5,0) circle (1cm);
    \fill[purple!50] (-0.5,0) circle (1cm);
  \end{scope}
  \node[label] at (0, -1.8) {RIGHT JOIN};
  \node[font=\tiny] at (-1.2, 0) {A};
  \node[font=\tiny] at (1.2, 0) {B};
  \node[font=\tiny] at (0.5, 0.5) {Tutti B};
\end{scope}

\end{tikzpicture}

\vspace{0.5cm}

\begin{tikzpicture}[
  box/.style={rectangle, draw, minimum width=4cm, minimum height=1cm, font=\small}
]

% Examples
\node[box, fill=purple!20] (inner) at (0, 0) {
  \begin{minipage}{3.8cm}
  \textbf{INNER JOIN}\\
  Solo righe con match in entrambe le tabelle
  \end{minipage}
};

\node[box, fill=blue!20, below=0.3cm of inner] (left) {
  \begin{minipage}{3.8cm}
  \textbf{LEFT JOIN}\\
  Tutte le righe da A + match da B (NULL se no match)
  \end{minipage}
};

\node[box, fill=red!20, below=0.3cm of left] (right) {
  \begin{minipage}{3.8cm}
  \textbf{RIGHT JOIN}\\
  Tutte le righe da B + match da A (NULL se no match)
  \end{minipage}
};

\end{tikzpicture}
\end{center}

\subsubsection{Visualizzazione: Esempio Query con JOIN}

\begin{tcolorbox}[colback=black!5, colframe=black!75, title=Query SELECT con JOIN e risultati, fonttitle=\bfseries\ttfamily]
\begin{verbatim}
sqlite> SELECT users.username, posts.title
        FROM posts
        INNER JOIN users ON posts.user_id = users.id;
username|title
alice|My First Post
alice|Python Tutorial
bob|Database Guide
charlie|Web Development

sqlite> SELECT users.username, COUNT(posts.id) as post_count
        FROM users
        LEFT JOIN posts ON users.id = posts.user_id
        GROUP BY users.id;
username|post_count
alice|2
bob|1
charlie|1
diana|0
\end{verbatim}
\end{tcolorbox}

\subsection{UPDATE e DELETE}

\begin{lstlisting}
import sqlite3

conn = sqlite3.connect("blog.db")
cursor = conn.cursor()

# UPDATE
cursor.execute(
    "UPDATE users SET email = ? WHERE username = ?",
    ("newemail@example.com", "alice")
)

affected_rows = cursor.rowcount
print(f"Updated {affected_rows} row(s)")

# DELETE
cursor.execute("DELETE FROM posts WHERE id = ?", (5,))

if cursor.rowcount > 0:
    print("Post deleted")
else:
    print("Post not found")

conn.commit()
conn.close()
\end{lstlisting}

\subsubsection{Visualizzazione: Operazioni CRUD in SQLite}

\begin{tcolorbox}[colback=black!5, colframe=black!75, title=Esempi UPDATE e DELETE, fonttitle=\bfseries\ttfamily]
\begin{verbatim}
$ python update_delete.py
Updated 1 row(s)
Post deleted

sqlite> SELECT * FROM users WHERE username = 'alice';
id|username|email|password_hash|created_at
1|alice|newemail@example.com|hashed_password|2024-01-15 10:30:45

sqlite> SELECT COUNT(*) FROM posts;
3
\end{verbatim}
\end{tcolorbox}

\subsection{Transazioni}

\begin{lstlisting}
import sqlite3

conn = sqlite3.connect("blog.db")
cursor = conn.cursor()

try:
    # BEGIN TRANSACTION (implicito)
    cursor.execute(
        "INSERT INTO users (username, email, password_hash) VALUES (?, ?, ?)",
        ("eve", "eve@example.com", "hash")
    )

    user_id = cursor.lastrowid

    cursor.execute(
        "INSERT INTO posts (user_id, title, content) VALUES (?, ?, ?)",
        (user_id, "First Post", "Hello World!")
    )

    # Simula errore
    # raise ValueError("Something went wrong")

    # COMMIT se tutto OK
    conn.commit()
    print("Transaction committed successfully")

except Exception as e:
    # ROLLBACK se errore
    conn.rollback()
    print(f"Transaction rolled back: {e}")

finally:
    conn.close()
\end{lstlisting}

\section{SQLAlchemy: ORM Potente}

\subsection{Installazione e setup}

\begin{lstlisting}[language=bash]
pip install sqlalchemy
\end{lstlisting}

\subsection{Definizione modelli}

\begin{lstlisting}
from sqlalchemy import Column, Integer, String, Text, DateTime, ForeignKey, create_engine
from sqlalchemy.orm import declarative_base, relationship, sessionmaker
from datetime import datetime

Base = declarative_base()

class User(Base):
    __tablename__ = "users"

    id = Column(Integer, primary_key=True)
    username = Column(String(50), unique=True, nullable=False)
    email = Column(String(100), unique=True, nullable=False)
    password_hash = Column(String(255), nullable=False)
    created_at = Column(DateTime, default=datetime.utcnow)

    # Relationship (One-to-Many)
    posts = relationship("Post", back_populates="author", cascade="all, delete-orphan")

    def __repr__(self):
        return f"<User(id={self.id}, username='{self.username}')>"

class Post(Base):
    __tablename__ = "posts"

    id = Column(Integer, primary_key=True)
    user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    title = Column(String(200), nullable=False)
    content = Column(Text, nullable=False)
    created_at = Column(DateTime, default=datetime.utcnow)

    # Relationship (Many-to-One)
    author = relationship("User", back_populates="posts")

    def __repr__(self):
        return f"<Post(id={self.id}, title='{self.title}')>"

# Crea engine e tabelle
engine = create_engine("sqlite:///blog.db", echo=True)  # echo=True mostra SQL
Base.metadata.create_all(engine)

# Crea Session factory
Session = sessionmaker(bind=engine)
\end{lstlisting}

\subsubsection{Visualizzazione: Entity-Relationship Diagram Completo}

\begin{center}
\begin{tikzpicture}[
  entity/.style={rectangle, draw=blue!60, fill=blue!10, thick, minimum width=4cm, minimum height=1cm, font=\bfseries},
  attribute/.style={rectangle, draw=gray!60, fill=white, minimum width=4cm, minimum height=0.5cm, font=\small},
  pk/.style={fill=yellow!20},
  fk/.style={fill=green!10},
  rel/.style={diamond, draw=purple!60, fill=purple!10, thick, font=\small}
]

% User Entity
\node[entity] (user) at (0, 0) {User};
\node[attribute, pk, below=0cm of user] (user_id) {\texttt{id}: Integer (PK)};
\node[attribute, below=0cm of user_id] (user_username) {\texttt{username}: String};
\node[attribute, below=0cm of user_username] (user_email) {\texttt{email}: String};
\node[attribute, below=0cm of user_email] (user_pass) {\texttt{password\_hash}: String};
\node[attribute, below=0cm of user_pass] (user_created) {\texttt{created\_at}: DateTime};

% Post Entity
\node[entity] (post) at (8, 0) {Post};
\node[attribute, pk, below=0cm of post] (post_id) {\texttt{id}: Integer (PK)};
\node[attribute, fk, below=0cm of post_id] (post_user) {\texttt{user\_id}: Integer (FK)};
\node[attribute, below=0cm of post_user] (post_title) {\texttt{title}: String};
\node[attribute, below=0cm of post_title] (post_content) {\texttt{content}: Text};
\node[attribute, below=0cm of post_content] (post_created) {\texttt{created\_at}: DateTime};

% Relationship
\node[rel] (writes) at (4, -2) {scrive};
\draw[->, thick] (user_created.east) -| (writes);
\draw[->, thick] (writes) |- (post_user.west);
\node[above, font=\tiny] at (2, -2) {1};
\node[above, font=\tiny] at (6, -2) {N};

% Tag Entity (for Many-to-Many)
\node[entity] (tag) at (8, -6.5) {Tag};
\node[attribute, pk, below=0cm of tag] (tag_id) {\texttt{id}: Integer (PK)};
\node[attribute, below=0cm of tag_id] (tag_name) {\texttt{name}: String};

% Association table
\node[entity, fill=orange!10] (post_tag) at (4, -6.5) {post\_tags};
\node[attribute, fk, below=0cm of post_tag] (pt_post) {\texttt{post\_id}: Integer (FK)};
\node[attribute, fk, below=0cm of pt_post] (pt_tag) {\texttt{tag\_id}: Integer (FK)};

% Many-to-Many relationships
\draw[->, thick] (post_created.south) -- ++(0,-1) -| (pt_post.north);
\draw[->, thick] (pt_tag.east) -- (tag_id.west);
\node[left, font=\tiny] at (7.5, -6) {N};
\node[right, font=\tiny] at (5, -6) {N};

\end{tikzpicture}
\end{center}

\subsection{CRUD Operations con SQLAlchemy}

\begin{lstlisting}
from sqlalchemy.orm import sessionmaker

Session = sessionmaker(bind=engine)
session = Session()

# CREATE
alice = User(
    username="alice",
    email="alice@example.com",
    password_hash="hashed_password"
)

session.add(alice)
session.commit()

print(f"Created user: {alice.id}")

# CREATE con relationship
post = Post(
    title="My First Post",
    content="Hello World!",
    author=alice  # Imposta relationship automaticamente
)

session.add(post)
session.commit()

# READ - query singolo
user = session.query(User).filter_by(username="alice").first()
print(user)

# READ - query multipli
users = session.query(User).all()
for user in users:
    print(f"{user.username} - {user.email}")

# READ con WHERE
recent_posts = session.query(Post).filter(
    Post.created_at > datetime(2024, 1, 1)
).order_by(Post.created_at.desc()).all()

# READ con JOIN (automatico via relationship)
user = session.query(User).filter_by(username="alice").first()
for post in user.posts:  # Lazy loading
    print(f"Post: {post.title}")

# UPDATE
user = session.query(User).filter_by(username="alice").first()
user.email = "newemail@example.com"
session.commit()

# DELETE
post = session.query(Post).filter_by(id=1).first()
if post:
    session.delete(post)
    session.commit()

session.close()
\end{lstlisting}

\subsubsection{Visualizzazione: SQLAlchemy ORM Query Flow}

\begin{center}
\begin{tikzpicture}[
  node distance=1.2cm,
  box/.style={rectangle, draw=blue!60, fill=blue!10, thick, minimum width=3cm, minimum height=0.8cm, font=\small, align=center},
  process/.style={rectangle, draw=green!60, fill=green!10, thick, minimum width=3cm, minimum height=0.8cm, font=\small, align=center},
  db/.style={cylinder, shape border rotate=90, draw=red!60, fill=red!10, thick, minimum width=2cm, minimum height=1cm, font=\small, align=center},
  arrow/.style={->, thick, >=stealth}
]

% User code
\node[box] (code) {Codice Python\\session.query(User)};

% SQLAlchemy ORM
\node[process, below=of code] (orm) {SQLAlchemy ORM\\Costruisce query};

% SQL Generation
\node[process, below=of orm] (sql) {Genera SQL\\SELECT * FROM users};

% Database Engine
\node[db, below=of sql] (engine) {SQLite\\Engine};

% Execute
\node[process, below=of engine] (exec) {Esegue Query\\Ritorna righe};

% Map to Objects
\node[process, below=of exec] (map) {Mappa a Oggetti\\User instances};

% Return
\node[box, below=of map] (result) {Risultato\\Lista di User};

% Arrows
\draw[arrow, blue!70] (code) -- (orm);
\draw[arrow, green!70] (orm) -- (sql);
\draw[arrow, green!70] (sql) -- (engine);
\draw[arrow, red!70] (engine) -- (exec);
\draw[arrow, green!70] (exec) -- (map);
\draw[arrow, blue!70] (map) -- (result);

% Side annotations
\node[right=1.5cm of orm, font=\tiny, text width=3cm] {
  \textbf{Layer ORM}\\
  Trasforma chiamate\\
  Pythonic in SQL
};

\node[right=1.5cm of engine, font=\tiny, text width=3cm] {
  \textbf{Database}\\
  Esegue query\\
  e ritorna dati raw
};

\node[right=1.5cm of map, font=\tiny, text width=3cm] {
  \textbf{Mapping}\\
  Righe SQL $\rightarrow$\\
  Oggetti Python
};

\end{tikzpicture}
\end{center}

\subsubsection{Visualizzazione: Esempio Output SQLAlchemy}

\begin{tcolorbox}[colback=black!5, colframe=black!75, title=SQLAlchemy ORM con echo=True, fonttitle=\bfseries\ttfamily]
\begin{verbatim}
$ python sqlalchemy_demo.py
INFO:sqlalchemy.engine.Engine:BEGIN
INFO:sqlalchemy.engine.Engine:INSERT INTO users
  (username, email, password_hash, created_at)
  VALUES (?, ?, ?, ?)
INFO:sqlalchemy.engine.Engine:('alice', 'alice@example.com',
  'hashed_password', '2024-01-15 10:30:45.123456')
INFO:sqlalchemy.engine.Engine:COMMIT

Created user: 1

INFO:sqlalchemy.engine.Engine:SELECT users.id, users.username,
  users.email, users.password_hash, users.created_at
  FROM users WHERE users.username = ?
INFO:sqlalchemy.engine.Engine:('alice',)

<User(id=1, username='alice')>
\end{verbatim}
\end{tcolorbox}

\subsection{Query avanzate}

\begin{lstlisting}
from sqlalchemy import func, and_, or_

session = Session()

# COUNT
user_count = session.query(func.count(User.id)).scalar()
print(f"Total users: {user_count}")

# JOIN esplicito
results = session.query(User, Post).join(Post).filter(
    User.username == "alice"
).all()

for user, post in results:
    print(f"{user.username}: {post.title}")

# AND / OR
users = session.query(User).filter(
    and_(
        User.username.like("a%"),
        User.created_at > datetime(2024, 1, 1)
    )
).all()

# IN
usernames = ["alice", "bob", "charlie"]
users = session.query(User).filter(User.username.in_(usernames)).all()

# ORDER BY e LIMIT
top_posts = session.query(Post).order_by(
    Post.created_at.desc()
).limit(10).all()

# GROUP BY e HAVING
from sqlalchemy import func

stats = session.query(
    User.username,
    func.count(Post.id).label("post_count")
).join(Post).group_by(User.id).having(
    func.count(Post.id) > 5
).all()

for username, count in stats:
    print(f"{username}: {count} posts")

session.close()
\end{lstlisting}

\subsection{Relationships: Many-to-Many}

\begin{lstlisting}
from sqlalchemy import Table

# Association table per Many-to-Many
post_tags = Table(
    "post_tags",
    Base.metadata,
    Column("post_id", Integer, ForeignKey("posts.id"), primary_key=True),
    Column("tag_id", Integer, ForeignKey("tags.id"), primary_key=True)
)

class Tag(Base):
    __tablename__ = "tags"

    id = Column(Integer, primary_key=True)
    name = Column(String(50), unique=True, nullable=False)

    # Many-to-Many relationship
    posts = relationship("Post", secondary=post_tags, back_populates="tags")

    def __repr__(self):
        return f"<Tag(name='{self.name}')>"

# Modifica Post model per aggiungere tags
class Post(Base):
    # ... (campi esistenti) ...

    tags = relationship("Tag", secondary=post_tags, back_populates="posts")

# Uso
session = Session()

# Crea tags
python_tag = Tag(name="python")
database_tag = Tag(name="database")

session.add_all([python_tag, database_tag])

# Crea post con tags
post = Post(
    title="SQLAlchemy Tutorial",
    content="Learn SQLAlchemy...",
    author=alice,
    tags=[python_tag, database_tag]  # Many-to-Many
)

session.add(post)
session.commit()

# Query posts by tag
posts_with_python = session.query(Post).join(Post.tags).filter(
    Tag.name == "python"
).all()

session.close()
\end{lstlisting}

\subsection{Evitare N+1 Query Problem}

\subsubsection{Visualizzazione: N+1 Query Problem}

\begin{center}
\begin{tikzpicture}[
  node distance=0.8cm and 2cm,
  query/.style={rectangle, draw=red!60, fill=red!10, thick, minimum width=4cm, minimum height=0.6cm, font=\footnotesize},
  queryopt/.style={rectangle, draw=green!60, fill=green!10, thick, minimum width=4cm, minimum height=0.6cm, font=\footnotesize},
  label/.style={font=\small\bfseries}
]

% Problem: N+1 queries
\node[label] (prob_title) at (0, 0) {PROBLEMA: N+1 Queries};

\node[query, below=0.3cm of prob_title] (q1) {Query 1: SELECT * FROM users};
\node[query, below=0.2cm of q1] (q2) {Query 2: SELECT * FROM posts WHERE user\_id=1};
\node[query, below=0.2cm of q2] (q3) {Query 3: SELECT * FROM posts WHERE user\_id=2};
\node[query, below=0.2cm of q3] (q4) {Query 4: SELECT * FROM posts WHERE user\_id=3};
\node[below=0.2cm of q4, font=\footnotesize] (dots1) {...};
\node[query, below=0.2cm of dots1] (qn) {Query N+1: SELECT * FROM posts WHERE user\_id=N};

\node[below=0.3cm of qn, font=\small, text width=4cm, align=center] (prob_desc) {
  \textbf{1 + N queries}\\
  Molto lento con molti utenti!
};

% Solution: Eager loading
\node[label, right=3cm of prob_title] (sol_title) {SOLUZIONE: Eager Loading};

\node[queryopt, below=0.3cm of sol_title] (sq1) {Query 1: SELECT * FROM users};
\node[queryopt, below=0.2cm of sq1] (sq2) {Query 2: SELECT * FROM posts\\WHERE user\_id IN (1,2,3,...,N)};

\node[below=1.8cm of sq2, font=\small, text width=4cm, align=center] (sol_desc) {
  \textbf{Solo 2 queries}\\
  joinedload() o selectinload()
};

% Arrow showing improvement
\draw[->, thick, blue!70, dashed] (prob_desc.east) to[bend right=20] node[below, font=\tiny] {Ottimizzazione} (sol_desc.west);

\end{tikzpicture}
\end{center}

\begin{lstlisting}
from sqlalchemy.orm import joinedload, selectinload

session = Session()

# PROBLEMA: N+1 queries (inefficiente!)
users = session.query(User).all()
for user in users:  # 1 query
    for post in user.posts:  # N queries (una per user!)
        print(post.title)

# SOLUZIONE 1: joinedload (INNER JOIN, 1 query)
users = session.query(User).options(
    joinedload(User.posts)
).all()

for user in users:
    for post in user.posts:  # Nessuna query aggiuntiva!
        print(post.title)

# SOLUZIONE 2: selectinload (2 queries totali)
users = session.query(User).options(
    selectinload(User.posts)
).all()

# Con nested relationships
users = session.query(User).options(
    joinedload(User.posts).joinedload(Post.tags)
).all()

session.close()
\end{lstlisting}

\subsection{Transazioni e gestione errori}

\begin{lstlisting}
from sqlalchemy.exc import IntegrityError

session = Session()

try:
    # Transazione complessa
    user = User(username="frank", email="frank@example.com", password_hash="hash")
    session.add(user)
    session.flush()  # Esegue INSERT ma non commit

    post = Post(title="Test", content="Content", author=user)
    session.add(post)

    # Simula errore
    # raise ValueError("Something went wrong")

    session.commit()  # Commit tutto
    print("Transaction successful")

except IntegrityError as e:
    session.rollback()
    print(f"Integrity error (es. UNIQUE constraint): {e}")

except Exception as e:
    session.rollback()
    print(f"Transaction failed: {e}")

finally:
    session.close()

# Context manager (raccomandato)
with Session() as session:
    with session.begin():
        user = User(username="grace", email="grace@example.com", password_hash="hash")
        session.add(user)
        # Auto-commit se no eccezioni, auto-rollback se errore
\end{lstlisting}

\subsubsection{Visualizzazione: Flusso Transazioni Database}

\begin{center}
\begin{tikzpicture}[
  node distance=1cm,
  start/.style={circle, draw=green!60, fill=green!20, thick, minimum size=1cm, font=\small},
  end/.style={circle, draw=blue!60, fill=blue!20, thick, minimum size=1cm, font=\small},
  error/.style={circle, draw=red!60, fill=red!20, thick, minimum size=1cm, font=\small},
  operation/.style={rectangle, draw=purple!60, fill=purple!10, thick, minimum width=2.5cm, minimum height=0.8cm, font=\footnotesize, align=center},
  decision/.style={diamond, draw=orange!60, fill=orange!10, thick, minimum size=1.5cm, font=\footnotesize, align=center, aspect=2},
  arrow/.style={->, thick, >=stealth}
]

% Start
\node[start] (begin) at (0, 0) {BEGIN};

% Operations
\node[operation, below=of begin] (op1) {INSERT INTO users};
\node[operation, below=of op1] (op2) {INSERT INTO posts};
\node[operation, below=of op2] (op3) {UPDATE users};

% Decision
\node[decision, below=of op3] (check) {Errori?};

% Commit
\node[end, below left=1.5cm and 2cm of check] (commit) {COMMIT};

% Rollback
\node[error, below right=1.5cm and 2cm of check] (rollback) {ROLLBACK};

% Success path
\draw[arrow, green!70] (begin) -- (op1);
\draw[arrow, green!70] (op1) -- (op2);
\draw[arrow, green!70] (op2) -- (op3);
\draw[arrow, orange!70] (op3) -- (check);

% Decision branches
\draw[arrow, green!70] (check) -- node[left, font=\tiny] {NO} (commit);
\draw[arrow, red!70] (check) -- node[right, font=\tiny] {SI} (rollback);

% Annotations
\node[below=0.3cm of commit, font=\tiny, text width=2.5cm, align=center] {
  Tutte le modifiche\\
  vengono salvate
};

\node[below=0.3cm of rollback, font=\tiny, text width=2.5cm, align=center] {
  Tutte le modifiche\\
  vengono annullate
};

\end{tikzpicture}
\end{center}

\section{Migrazioni con Alembic}

\subsection{Setup Alembic}

\begin{lstlisting}[language=bash]
pip install alembic

# Inizializza Alembic
alembic init alembic

# Modifica alembic.ini: imposta sqlalchemy.url
# sqlalchemy.url = sqlite:///blog.db

# Modifica alembic/env.py: importa Base
# from myapp.models import Base
# target_metadata = Base.metadata
\end{lstlisting}

\subsection{Creazione migration}

\begin{lstlisting}[language=bash]
# Crea migration automatica (da differenza modelli)
alembic revision --autogenerate -m "Add users and posts tables"

# Applica migration
alembic upgrade head

# Rollback migration
alembic downgrade -1

# Storia migrazioni
alembic history
\end{lstlisting}

\subsubsection{Visualizzazione: Workflow Alembic Migrations}

\begin{center}
\begin{tikzpicture}[
  node distance=1.2cm,
  step/.style={rectangle, draw=blue!60, fill=blue!10, thick, minimum width=4cm, minimum height=0.8cm, font=\small, align=center},
  file/.style={rectangle, draw=green!60, fill=green!10, thick, minimum width=3cm, minimum height=0.6cm, font=\footnotesize, align=center},
  db/.style={cylinder, shape border rotate=90, draw=red!60, fill=red!10, thick, minimum width=2cm, minimum height=0.8cm, font=\small, align=center},
  arrow/.style={->, thick, >=stealth}
]

% Steps
\node[step] (init) at (0, 0) {1. alembic init alembic};
\node[step, below=of init] (modify) {2. Modifica modelli\\SQLAlchemy};
\node[step, below=of modify] (autogen) {3. alembic revision\\--autogenerate};
\node[file, below=of autogen] (migration) {Migration file\\xxx\_add\_table.py};
\node[step, below=of migration] (review) {4. Revisiona migration\\(upgrade/downgrade)};
\node[step, below=of review] (upgrade) {5. alembic upgrade head};
\node[db, below=of upgrade] (database) {Database\\Aggiornato};

% Arrows
\draw[arrow, blue!70] (init) -- (modify);
\draw[arrow, blue!70] (modify) -- (autogen);
\draw[arrow, green!70] (autogen) -- (migration);
\draw[arrow, orange!70] (migration) -- (review);
\draw[arrow, blue!70] (review) -- (upgrade);
\draw[arrow, red!70] (upgrade) -- (database);

% Side annotations
\node[right=1cm of autogen, font=\tiny, text width=3cm, align=left] {
  Alembic confronta\\
  modelli con schema\\
  DB e genera migration
};

\node[right=1cm of upgrade, font=\tiny, text width=3cm, align=left] {
  Applica le modifiche\\
  allo schema database\\
  in modo controllato
};

\end{tikzpicture}
\end{center}

\subsubsection{Visualizzazione: Esempio Alembic Commands}

\begin{tcolorbox}[colback=black!5, colframe=black!75, title=Comandi Alembic, fonttitle=\bfseries\ttfamily]
\begin{verbatim}
$ alembic init alembic
Creating directory /project/alembic ... done
Creating directory /project/alembic/versions ... done
Generating /project/alembic.ini ... done
Generating /project/alembic/env.py ... done

$ alembic revision --autogenerate -m "Add users table"
INFO [alembic.runtime.migration] Context impl SQLiteImpl.
INFO [alembic.autogenerate.compare] Detected added table 'users'
Generating /project/alembic/versions/001_add_users_table.py

$ alembic upgrade head
INFO [alembic.runtime.migration] Running upgrade  -> 001
INFO [alembic.runtime.migration] Running upgrade 001 -> head

$ alembic current
001 (head)

$ alembic downgrade -1
INFO [alembic.runtime.migration] Running downgrade 001 ->
\end{verbatim}
\end{tcolorbox}

\subsection{Migration manuale}

\begin{lstlisting}
# File: alembic/versions/xxxx_add_bio_to_users.py

from alembic import op
import sqlalchemy as sa

def upgrade():
    # Aggiungi colonna
    op.add_column("users", sa.Column("bio", sa.Text, nullable=True))

    # Crea indice
    op.create_index("idx_email", "users", ["email"])

def downgrade():
    # Rimuovi indice
    op.drop_index("idx_email", table_name="users")

    # Rimuovi colonna
    op.drop_column("users", "bio")
\end{lstlisting}

\begin{tcolorbox}[title=Best Practices]
L'uso di prepared statements è assolutamente obbligatorio per evitare SQL injection, uno dei rischi di sicurezza più gravi nelle applicazioni web. Mai concatenare input dell'utente direttamente nelle query SQL.

Chiudere sempre connessioni e sessioni database è fondamentale per evitare resource leak. L'uso di context manager con \verb|with| garantisce cleanup automatico anche in caso di eccezioni.

Le transazioni devono essere usate per operazioni multi-step che devono essere atomiche: o tutte le operazioni hanno successo, o tutte vengono annullate con rollback, garantendo consistenza dei dati.

Evitare il problema N+1 queries è critico per le performance. Usare \verb|joinedload| o \verb|selectinload| in SQLAlchemy carica i dati correlati in anticipo, evitando query aggiuntive per ogni elemento.

L'indicizzazione delle colonne usate frequentemente in WHERE, JOIN e ORDER BY è essenziale per query veloci. Senza indici appropriati, le query su tabelle grandi diventano inaccettabilmente lente.

Alembic dovrebbe essere usato per tutte le migrazioni di schema, evitando modifiche manuali con ALTER TABLE che possono causare inconsistenze tra ambienti.

La gestione esplicita di eccezioni come \verb|IntegrityError| (violazioni di vincoli) e \verb|OperationalError| (problemi di connessione) permette di gestire errori in modo appropriato anziché crashare.

Testare query complesse con EXPLAIN QUERY PLAN aiuta a identificare bottleneck e verificare che gli indici vengano utilizzati correttamente dall'optimizer.

Il connection pooling in produzione riusa connessioni esistenti invece di crearne nuove per ogni richiesta, migliorando significativamente le performance.

Infine, backup regolari del database sono essenziali per disaster recovery, proteggendo da corruzioni, cancellazioni accidentali e guasti hardware.
\end{tcolorbox}

\begin{tcolorbox}[title=Errori Comuni]
L'uso di f-strings per costruire query SQL è un errore gravissimo che apre vulnerabilità a SQL injection. Sempre usare placeholder (?) o prepared statements per parametrizzare le query.

Dimenticare di chiamare \verb|commit()| dopo INSERT, UPDATE o DELETE è un errore comune che fa sì che le modifiche non vengano persistite, causando perdita di dati.

Non chiudere sessioni database porta a resource leak che esaurisce il connection pool, causando errori "too many connections" in produzione.

Il problema N+1 queries con lazy loading degrada drasticamente le performance: caricare 100 utenti con i loro post genera 101 query invece di 2, rallentando l'applicazione.

Non gestire \verb|IntegrityError| per violazioni di vincoli UNIQUE e foreign key porta a crash invece di fornire messaggi di errore utili all'utente.

Usare \verb|.all()| per caricare grandi dataset in memoria causa problemi di memoria e performance. Per dataset grandi, usare pagination con limit/offset.

Non creare indici sulle colonne usate frequentemente nelle query causa scan completi di tabelle, rendendo le query estremamente lente su tabelle grandi.

Modificare lo schema del database manualmente senza migration crea inconsistenze tra ambienti (dev/staging/prod), causando bug difficili da diagnosticare.

Non usare transazioni per operazioni critiche multi-step rischia di lasciare il database in uno stato inconsistente se un'operazione fallisce a metà.

Infine, esporre oggetti SQLAlchemy direttamente come risposta API crea problemi di serializzazione e accoppiamento. Usare sempre DTO o serializer per separare la rappresentazione interna da quella esterna.
\end{tcolorbox}

\section{Pattern e Best Practices}

\subsection{Repository Pattern}

\subsubsection{Visualizzazione: Architecture Pattern Repository}

\begin{center}
\begin{tikzpicture}[
  node distance=1.5cm,
  layer/.style={rectangle, draw, thick, minimum width=6cm, minimum height=1.2cm, font=\small, align=center},
  app/.style={fill=blue!20, draw=blue!60},
  repo/.style={fill=green!20, draw=green!60},
  orm/.style={fill=orange!20, draw=orange!60},
  db/.style={fill=red!20, draw=red!60},
  arrow/.style={<->, thick, >=stealth}
]

% Layers
\node[layer, app] (application) at (0, 0) {\textbf{Application Layer}\\Controllers, Services};
\node[layer, repo, below=of application] (repository) {\textbf{Repository Layer}\\UserRepository, PostRepository};
\node[layer, orm, below=of repository] (sqlalchemy) {\textbf{ORM Layer}\\SQLAlchemy Session, Models};
\node[layer, db, below=of sqlalchemy] (database) {\textbf{Database}\\SQLite / PostgreSQL};

% Arrows
\draw[arrow, blue!70] (application) -- (repository);
\draw[arrow, green!70] (repository) -- (sqlalchemy);
\draw[arrow, red!70] (sqlalchemy) -- (database);

% Annotations
\node[right=0.5cm of repository, font=\tiny, text width=3.5cm, align=left] {
  \textbf{Vantaggi:}\\
  - Separa logica business da DB\\
  - Facilita testing (mock repo)\\
  - Centralizza query DB\\
  - Riduce codice duplicato
};

\end{tikzpicture}
\end{center}

\begin{lstlisting}
from typing import Optional, List
from sqlalchemy.orm import Session

class UserRepository:
    def __init__(self, session: Session):
        self.session = session

    def getById(self, userId: int) -> Optional[User]:
        return self.session.query(User).filter_by(id=userId).first()

    def getByUsername(self, username: str) -> Optional[User]:
        return self.session.query(User).filter_by(username=username).first()

    def getAll(self, limit: int = 100, offset: int = 0) -> List[User]:
        return self.session.query(User).limit(limit).offset(offset).all()

    def create(self, username: str, email: str, password_hash: str) -> User:
        user = User(username=username, email=email, password_hash=password_hash)
        self.session.add(user)
        self.session.commit()
        return user

    def update(self, user: User) -> User:
        self.session.commit()
        return user

    def delete(self, user: User) -> None:
        self.session.delete(user)
        self.session.commit()

# Uso
with Session() as session:
    repo = UserRepository(session)

    user = repo.create("henry", "henry@example.com", "hash")
    print(f"Created: {user.id}")

    found = repo.getByUsername("henry")
    print(f"Found: {found.email}")
\end{lstlisting}

\subsection{Database seeding}

\begin{lstlisting}
def seedDatabase():
    """Popola database con dati iniziali"""
    with Session() as session:
        # Verifica se già popolato
        if session.query(User).count() > 0:
            print("Database already seeded")
            return

        # Crea utenti
        users = [
            User(username="admin", email="admin@blog.com", password_hash="hash1"),
            User(username="alice", email="alice@blog.com", password_hash="hash2"),
            User(username="bob", email="bob@blog.com", password_hash="hash3"),
        ]

        session.add_all(users)
        session.commit()

        # Crea tags
        tags = [
            Tag(name="python"),
            Tag(name="database"),
            Tag(name="web"),
        ]

        session.add_all(tags)
        session.commit()

        # Crea posts
        posts = [
            Post(title="Python Tutorial", content="...", author=users[0], tags=[tags[0]]),
            Post(title="Database Guide", content="...", author=users[1], tags=[tags[1]]),
            Post(title="Web Development", content="...", author=users[2], tags=[tags[2]]),
        ]

        session.add_all(posts)
        session.commit()

        print("Database seeded successfully")

if __name__ == "__main__":
    seedDatabase()
\end{lstlisting}

\section{Troubleshooting}

\textbf{Problema}: IntegrityError: UNIQUE constraint failed
\begin{lstlisting}
# Gestisci duplicati
try:
    user = User(username="alice", email="alice@example.com", password_hash="hash")
    session.add(user)
    session.commit()
except IntegrityError:
    session.rollback()
    print("User already exists")
\end{lstlisting}

\textbf{Problema}: DetachedInstanceError (oggetto non in sessione)
\begin{lstlisting}
# Riattacca oggetto
session.add(user)  # Merge oggetto nella sessione

# Oppure usa expire_on_commit=False
Session = sessionmaker(bind=engine, expire_on_commit=False)
\end{lstlisting}

\textbf{Problema}: Query lente
\begin{lstlisting}[language=bash]
# Analizza query con EXPLAIN
# In SQLite
EXPLAIN QUERY PLAN SELECT * FROM posts WHERE user_id = 5;

# Aggiungi indici
CREATE INDEX idx_user_id ON posts(user_id);
\end{lstlisting}

\section{Esercizi}

\subsection{Livello Base}
\begin{enumerate}
  \item Crea database SQLite con tabella \texttt{books} (id, title, author, year)
  \item Inserisci 10 libri e query tutti con \texttt{SELECT *}
  \item Trova libri pubblicati dopo 2000 con prepared statement
\end{enumerate}

\subsection{Livello Intermedio}
\begin{enumerate}
  \item Definisci modelli SQLAlchemy per \texttt{Author} e \texttt{Book} (One-to-Many)
  \item Implementa CRUD completo con Repository pattern
  \item Aggiungi relationship Many-to-Many per \texttt{Book} e \texttt{Category}
  \item Query libri con \texttt{joinedload} per evitare N+1
\end{enumerate}

\subsection{Livello Avanzato}
\begin{enumerate}
  \item Implementa sistema blog completo (User, Post, Comment, Tag)
  \item Usa Alembic per creare migrations
  \item Implementa pagination per lista posts
  \item Aggiungi full-text search con FTS5 (SQLite)
  \item Crea seed script per popolare database di test
\end{enumerate}

\section{Riferimenti}
\begin{itemize}
  \item SQLite Documentation: \url{https://docs.python.org/3/library/sqlite3.html}
  \item SQLAlchemy Documentation: \url{https://docs.sqlalchemy.org/}
  \item Alembic Tutorial: \url{https://alembic.sqlalchemy.org/}
  \item SQL Tutorial: \url{https://www.w3schools.com/sql/}
\end{itemize}
